# 已知未修差异（诚实列表）

> **状态**: 指针（与 `BUSINESS.md` §2.2 同源）  
> **用途**: Agent 勿假装零差；**禁止**把本页写成待办诱惑大重构。  
> **禁止**: 本页不授权改 fusion / 出手 / 池分道 / 区间算法。

| 项 | 说明 |
|----|------|
| `report["wyckoff"]` 回退 | 周不足时可回退日线；面板 SSOT = `wyckoff_midline` / `wyckoff_daily` |
| 破位 `close is None` | 现码更严（与 structure-anchor「须 close」不完全一致） |
| 背驰展示 | 展示可 multi；一类扳机面积-only（formulas 取舍） |
| `zones_count` | 引擎 raw vs 卡 merged |

已修硬伤（forming 上沿、破位冷启动排除、ZG/ZD 极值）见 `BUSINESS.md` §2.2「区间定义要点」+ `done/range-diff-fixes-handoff.md`。
