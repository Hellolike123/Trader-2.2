# 短中线报告分区重组 — Agent Handoff

> **状态**: 规格冻结（用户 2026-08-01 对话确认）  
> **产品法源**: `BUSINESS.md` §5.1–5.2；`output-template.md`；威科夫量度闸见 `wyckoff-tr-maturity-l0l3-handoff.md` / `wyckoff-pnf-handoff.md`  
> **动机**: 扫读混乱——理论/关键价/出手搅在中短轨里；阶段+定论拼盘重复；P&F 与支撑阻力不同逻辑却易混读  
> **协作**: 业务 Agent 改契约文档；代码 Agent 改渲染+测；查 Agent 对照本文验收  

---

## 0. 本迭代范围

### 0.1 做

1. **面板主轴改按主题**：价格状态 → 理论分析 → 支撑阻力 → 门禁 → 尾部。  
2. **删除**中线「阶段：」行与「定论：」行（含拼装的「无阶段 · 偏空（…）」）；阶段信息只留在 `威科夫：` 正文。  
3. **原「关键价（中线/短线）」改名为「支撑阻力」分区**（中线子块 / 短线子块）；算法引擎不改。  
4. **P&F / 量度目标留在威科夫理论栏**（中线/短线各自轨下），**禁止**进支撑阻力区。  
5. **L3** 时威科夫下显式写出 `下沿 x｜上沿 y（L3）` + `量度目标：上 …｜下 …（P&F/高度1:1，非出手）`；L0–L2 **不贴**量度行（沉默省略，不写「未达 L3」解释行）。  
6. 同步 `BUSINESS.md` §5.1、`output-template.md`、golden、骨架断言测。  
7. **亮点/风险净化（续）**：尾部不得再拼装「中线阶段无阶段 · 偏空（…）」；偏空用中线看法短因（如「中线框破坏」），偏多用「中线可跟踪」类白话，禁止 `中线阶段{拼盘}` / `阶段 {stage_line}` 上屏。  
8. **中线 Fib 关闭**：`midline_structure` 主路径不再写「黄金买点」（50%回撤）、不再用 138.2% 抬高目标；恢复 `midline-price-engine-plan.md` §9.5（压力/目标仅周线笔段/摆动）。  
9. **短线支撑阻力展示净化**（南网裁决）：
   - **止损展示**：空仓 / 止损≥现价时，只用硬止损 `report.stop`；有仓且 `trailing_stop` **低于现价**时才可用移动止损（只紧不松）。禁止面板出现「止损在现价上方」。  
   - **点状低吸区**：若 `buy_zone_high - buy_zone_low` 相对价宽 < 0.15%（或两价四舍五入后相等），改标 `支撑 x`，禁止 `低吸区 x-x` 装区间。  
   - **组装 stop 入 `build_key_prices`**：与上同规（空仓传硬止损），避免 trailing 抬高污染低吸区评分。  
   - 风险/破位看里的「止损看」与面板止损同口径。  
   - 可改：`short_midline.py` 短线支撑阻力拼装、`attach_short_midline.py` 传入 stop 选择；golden/测。不大改 `structure_core` 硬/移动计算公式本身。
10. **门禁块读感（示意 A + 改名 + 价格协调）**（用户 2026-08-01 确认）：
   - 分区标题：`✅ 出手` → `✅ 门禁`（判定逻辑仍听 `decision_view`，只改展示名与行结构）。
   - 行结构固定为：
     ```text
     ✅ 门禁
       结论：不新开 · 仓 0%   （或 可试探 · 仓 x%）
       还差：…                 （全齐则省略）
       等待：…                 （无明确等待则省略）
       作废：…
       附：…                   （亏赚/不划算/偏冲高等；无可省略）
     ```
   - **还差**合并共振缺岗 + C1 缺项（去重）：岗位短名 `中线背景｜缠论｜筹码｜动能`；C1 映射 `买区｜买点｜把握不够｜筹码资金…`；已有「中线背景」则不再叠「中线未确认」；已有「筹码」则不再叠「筹码资金不稳」。超过 5 项时 `前5｜另N项`。
   - **价格协调（门禁不发明第三套价）**：
     1. 等待价：现价已破生命线 → `收回生命线 x（现价已破）`（优先于 confirm）；否则 confirm>现价 → `站稳 confirm`；再否则持「不追」语义可写 `不追现价`。
     2. 作废价：空仓只写 `破止损 x`（与短线支撑阻力止损同号）；**禁止**在现价已低于 MA20 时仍写「跌破MA20…站不回」。有仓可用纪律 invalidation，但须剥离已过期的 MA20 跌破话术。
     3. 中线支撑阻力：现价已破生命线时，生命行提示由「跌破则…」改为「已跌破」（展示层）。
   - 可改：`short_midline.py` 门禁拼装 + 生命线提示；`BUSINESS.md` §5.1、`output-template.md`、`agent-rules` 一句、golden/骨架测。  
   - **勿改**：`decision_view` / `mistery_gate` / `chan_discipline` 判定语义与 `format_entry_line_c1` 字段口径（门禁「还差」是展示合并，不替代 C1 字段）。

### 0.2 不做（后续迭代）

- 不改 `structure_core` 硬/移动止损公式本体（仅展示与传入 `build_key_prices` 的 stop 选择，见 §0.1-9）。  
- 不改 fusion / decision_view / 出手判定语义 / 池分道（§0.1-10 只改门禁展示）。  
- 短线低吸区改挂缠论结构（另开 handoff）。  
- 不改威科夫 skill 独立卡。  
- 不恢复已删除的「说明：」行。  
- 不强制改 meta「深成」→「深成指」（可选后续）。

---

## 1. 面板骨架（必须）

```text
分析报告 — {名}（{码}）｜短中线
（可选 ⚠️ 数据不完整）

📊 价格状态
  现价 …｜MA20 …｜MA250 …
  综合动能 … ｜ {板块短名} ±% ｜ {行业短名}? ±% ｜ 个股 ±%
  量比… ｜ 换手… ｜ 调整N天/创新高 ｜ ATR14 …（复权口径）
  （可选 ⏱盘中 / 概念题材 / 资金面 — 保持现逻辑）

📐 理论分析
  中线
    威科夫：…
    （仅 L3）下沿 x｜上沿 y（L3）
    （仅 L3）量度目标：上 x｜下 y（P&F，非出手）或（高度1:1，非出手）
    缠论：…
    （可选）位置：/ 筹码：/ 业绩预期：/ 附：股东户数 — 保持现可选行
  短线
    缠论：…
    （可选）买点：…（lifecycle）
    威科夫：…（仅对照）
    （仅日线 L3）下沿 x｜上沿 y（L3）
    （仅日线 L3）量度目标：…
    （可选）事件：…
    动能：…
    资金：…

🎯 支撑阻力
  中线
    … mid_key_prices 生命线/回踩/压力/目标（无 🌟；句式可保留现解释半句）
  短线
    … key_prices 止损/低吸或买点区/🌟现价/卖点区 + 亏赚两句
    （可选）日内 T0 / T0 行 — 挂短线支撑阻力后，保持现逻辑

✅ 门禁
  结论：…
  （可选）还差：…
  （可选）等待：…
  作废：…
  （可选）附：…

✅ 亮点：…
⚠️ 风险：…
📌 明日策略：… 或 📌 本周只做：…
（可选）T0：… — 若已在短线支撑阻力下输出则勿重复；以现码不双贴为准
当前池 n/m，回复 1 入池
```

### 1.1 禁止出现（本迭代）

- 中线块内独立行：`阶段：`、`定论：`  
- 支撑阻力区内：`量度目标` / `P&F` / `下沿…（L3）` 作为威科夫量度  
- 成功路径用日线 `key_levels` 冒充中线支撑阻力价（既有红线不变）  
- Markdown `#` / `**` / `|...|` 表 / `---` / `*/-` 列表符  

### 1.2 Emoji 标题（冻结）

| 区 | 标题行 |
|----|--------|
| 价格状态 | `📊 价格状态` |
| 理论分析 | `📐 理论分析` |
| 支撑阻力 | `🎯 支撑阻力` |
| 门禁 | `✅ 门禁` |

子块标签用缩进纯文：`中线` / `短线`（不加第二套 emoji 轨标 🧭/⚡，避免双层主轴）。

---

## 2. 字段与来源（不改引擎语义）

| 展示 | 字段来源 | 备注 |
|------|----------|------|
| 价格状态 meta | 现有 meta 拼装 | 纯 D 不变 |
| 威科夫中/短 | `format_wyckoff_*` / 现有行 | L1 雏形、L2/L3 箱体词规则不变 |
| L3 下沿/上沿 | `_phase_a_box_bounds` 或 View 已有 lo/hi | **仅** `tr_maturity==L3`（或 measure_allowed）展示分行；可与「箱体 lo-hi」并存 |
| 量度目标 | `format_cause_effect_display` | 已有闸；挪到威科夫行下（若现已在中线块则随理论区走） |
| 支撑阻力中线 | `mid_key_prices` | 禁止改 fill 逻辑 |
| 支撑阻力短线 | `key_prices` | 展示标签可将「买点区」读作「低吸区」**仅文案**（若改须同步 template）；算法字段名可保留 |
| 门禁块 | `decision_view` / resonance / discipline + 价位指针 | 展示重组见 §0.1-10；判定语义不改 |

---

## 3. 可改 / 勿改文件

### 可改

- `02-共享模块-shared/trader_shared/report_renderer/short_midline.py`  
- `01-功能包-packages/trader/references/output-template.md`  
- `BUSINESS.md` §5.1（骨架同步）  
- `01-功能包-packages/_common/agent-rules.md`（若列了旧 🧭/⚡ 主轴，改一句）  
- `02-共享模块-shared/tests/golden/*.render.md`  
- 依赖骨架的 pytest（如 `test_report_*` / render 断言）  
- 本 handoff  

### 勿改

- `midline_structure.py` / `mid_key_prices.py` 主算法  
- `key_prices.py` / `structure_core.py` 主算法（除非纯展示字符串映射）  
- `fusion_core.py` / `decision_view.py` / `mistery_gate.py` / 池分道  
- `wyckoff_core.py` L0–L3 门禁语义（只消费展示）  

---

## 4. 验收表

| ID | 必须 | 测/验 |
|----|------|-------|
| R-R1 | 面板顺序：价格状态 → 理论分析 → 支撑阻力 → 门禁 → 尾部 | golden + 断言 |
| R-R2 | 无 `阶段：` / `定论：` 独立行 | golden + 断言 |
| R-R2b | 亮点/风险无 `中线阶段…` 拼盘、无 `阶段 {stage_line}` | 单测 + 南网类 fixture |
| R-R3 | 无 🧭/⚡ 作主分区标题（改主题 emoji） | golden |
| R-R4 | 量度目标若出现，必在理论分析·威科夫下，不在支撑阻力 | 单测/golden |
| R-R5 | L0–L2 无量度行；L3 有下沿/上沿分行 + 量度行（fixture） | 单测 |
| R-R6 | 中线支撑阻力仍来自 `mid_key_prices`；短线来自 `key_prices` | 既有 isolation 测不破 |
| R-R6b | 中线无 Fib 黄金买点；目标不被 138.2% 抬高（component≠fib_*） | `test_midline_structure` |
| R-R11 | 空仓+trailing&gt;现价时，短线止损展示=硬止损且 &lt; 现价 | 单测（南网类） |
| R-R12 | 点状买区展示为「支撑」而非「低吸区 x-x」 | 单测 |
| R-R7 | 门禁块含 `结论：`；未齐时有 `还差：`；有作废线用 `作废：`（不再用共振/新开/动作/破位看旧骨架） | golden + 单测 |
| R-R13 | 现价&lt;生命线：等待=`收回生命线`；中线生命提示含「已跌破」；空仓作废不含过期 MA20 跌破语 | 单测 |
| R-R14 | 门禁等待/作废价能在支撑阻力区找到同号（生命线/止损） | 单测或目视 |
| R-R8 | 微信红线：无 `#`/`**`/管道表/`---` | 目视 + 既有格式测 |
| R-R9 | `BUSINESS.md` §5.1 与 `output-template.md` 与渲染一致 | 查 Agent |
| R-R10 | 门禁 `scripts/run-gate-tests.sh` 绿 | CI/本地 |

---

## 5. 双 Agent 分工

| 角色 | 职责 |
|------|------|
| **业务 Agent** | 只改文档契约：`BUSINESS.md` §5.1、`output-template.md`、必要时 `agent-rules` 一句；对照本文 §1 骨架，禁止改引擎 |
| **代码 Agent** | `short_midline.py` 重排 + L3 下沿/上沿展示 + golden/pytest；禁止改 fusion/出手判定 |
| **查 Agent** | 只读对照本文 §1 / §4 列 ✅/❌；默认不改码 |

父 Agent：查完修完再 PR。

---

## 6. 用户已确认的产品裁决（防忘）

1. 主题分区优于中短轨主轴。  
2. 阶段/定论拼盘删除更清晰。  
3. P&F 量度属威科夫栏；支撑阻力是另一套逻辑。  
4. ST 前只雏形、ST 后才箱体、L3 才量度——保持严格闸。  
5. L3 要显式下沿/上沿；量度上/下 ≠ 箱体下沿。  
6. 本迭代先落地展示重组；算法净化（Fib/低吸挂缠论）后续再改。
