---
name: trader-portfolio
description: Generate a validated A-share rotation and position plan by running scripts/final_portfolio.py. Requires terminal + python3. Return stdout verbatim with 规则版本：trader_portfolio_v1; never handwrite, summarize, shorten, or restyle the portfolio plan.
version: 0.2.0-v2-operation-card
author: Trader Skill
license: MIT
platforms: [macos, linux]
tags: [finance, stocks, a-share, portfolio, terminal, python]
metadata:
  hermes:
    tags: [Finance, AShare, Portfolio, Terminal, Python]
    requires_toolsets: [terminal]
  openclaw:
    requires:
      bins: [python3]
dependencies: [python3]
repository: local
documentation: SKILL.md
---

# Trader Portfolio

## Critical Rule
The portfolio plan is generated by code, not by the agent.

Run `scripts/final_portfolio.py --targets <股票1> <股票2> ...`, return stdout exactly, and do not compose a shorter answer from memory. If you cannot run the script, say `Trader Portfolio skill cannot run in this environment` and include the error.

The current contract marker is `规则版本：trader_portfolio_v1`. If output lacks this marker, it is not valid.

## Commands
```bash
python3 scripts/final_portfolio.py --targets 南网科技 中国铝业 三安光电
python3 scripts/final_portfolio.py --snapshot /path/to/portfolio_snapshot.json
python3 scripts/validate_output.py /path/to/portfolio.md
python3 scripts/self_check.py
```

## Output Contract
Default `--targets` output:

```text
轮动仓位 — xxx + xxx

🌍 大盘xx | 中证1000 xxx

📌 组合
    主仓  xxx  仓位 xx%  状态：xxx
    副仓  xxx  仓位 xx%  状态：xxx
    现金  xx%

🎯 操作
    xxx  加仓 ...  降仓 ...  止损 ...

📊 仓位与信号
    xxx  每天晃 x.xx元（x%）→ 波幅偏高，首仓≤x%

🎯 关键价位
    xxx  买入 xx-xx元  防守 xx元  止损 xx元  减仓 xx元

👉
    建议...
    利弗莫尔："..."
```

No markdown tables. Use indented alignment. ATR uses plain language (波幅偏高/偏大/正常/较低). Do not output `ATR14=`, `极端波动`, `高波动`, `低波动`.

## Old Output Detection
If the generated output contains `ATR14=`, markdown tables, `规则版本：` marker in the body text, or missing `规则版本：trader_portfolio_v1`, rerun the script.

## Rules
- If one stock fails, keep the plan for the others and mark that stock as `数据失败`.
- If all fail, return the failure.
- Without the `terminal` toolset, fail closed. Do not produce a portfolio plan from memory.
