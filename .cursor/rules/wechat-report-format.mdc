---
description: 单票/池子/T0/复盘面板贴给用户时必须用微信阅读纯文本格式
alwaysApply: true
---

# 微信阅读格式（贴报告必守）

把脚本 stdout 的面板贴给用户时：

1. **用 fenced code block（\`\`\`）整段包住**，避免 Cursor 把缩进/`|`/列表当 Markdown 渲染碎掉。
2. **只贴面板正文**（从 `分析报告 —` / 各 skill 首行起），不要贴 `__FACTS__`、`__CHECKLIST__`、pkg_resources 警告。
3. **正文本身遵守微信红线**（见 `01-功能包-packages/_common/agent-rules.md`）：
   - 禁 `#` 标题、`---`、`**` 粗体、`|...|` 表格、`>` 引用、`*`/`-` 列表符
   - 分节用 emoji 行（如 `🧭 中线`）；并列用全角 `｜`
4. **禁止改写脚本数字/结论**；需要解说时写在 code block **外面**，且仍勿用上述 Markdown。

反例：把报告当普通 Markdown 消息直接贴（聊天里会被渲染，拷到微信也不干净）。
正例：``` 包裹的整段脚本面板 → 用户一键复制进微信。
