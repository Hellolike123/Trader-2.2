---
description: 编程 Agent 必须按手递/BUSINESS 等法源开发；禁止凭感觉改结论
alwaysApply: true
---

# 按文档开发（改代码必守）

适用于改引擎 / 合同 / 面板契约（非「跑脚本贴报告」快路径）。

## 硬规则

1. **先法源，后代码。** 开工前读清并点名：
   - `BUSINESS.md` 相关节
   - `docs/plans/*-handoff.md`（验收表、禁止项、可改/勿改）
   - 需要时 `docs/audit/`、`docs/designs/`
2. **无 handoff 不写大改。** 缺合同先补手递（字段 / 必须禁止 / 测例 ID / 白名单），再实现。
3. **禁止发明。** 文档未写的行为（尤其出手、fusion、阶段抬升、假箱体/假量度）不得自行加上。
4. **禁止项优先。** 文档写「禁止 / 不得 / 须」的，代码与测例必须卡住；A 股只调参数，不砍原典语义。
5. **测例锁合同。** 「必须有测」→ pytest；改文档语义 → 先改测再改码。
6. **双 Agent 闭环（默认用于合同级改动）：**
   - 写 Agent：按法源实现 + 测 + push
   - 查 Agent：按同一法源验收，列 ❌/bug；修完再 PR
7. **PR 必带：** 法源链接、对照清单、查 Agent/自检结论、相关测试结果。

## 与快路径分工

- **跑票/贴面板**：只读 skill `agent-quickstart` → 跑脚本 → code fence 原样贴 → 停（见 `agent-rules` / 微信格式规则）。
- **改实现**：本规则 + `AGENTS.md`「按文档开发」+「改代码去哪」；勿凭训练记忆拼旧面板或旧威科夫语义。

## 反例 / 正例

- 反例：没读 L0–L3 handoff 就让 SC+AR 写「箱体」并出量度。
- 反例：查都没查就改文档迁就坏代码。
- 正例：点名 `wyckoff-tr-maturity-l0l3-handoff.md` §1.3 → 实现破位整段失败 → 查 Agent 对照 §1.3 / M-R* → 修完再 PR。
