# 2026-08-08 工作日志

## 只读「查 Agent」五项交叉核对审计（commit 0c58ed6）

作为只读查 Agent 对 report/wyckoff/chan 链做交叉核对审计，不改任何代码。

### 验证跑过的测试
- wyckoff_core + report_mid_short_sources: 179 passed
- chan_core + chan_segments_bug_r/no_overcut/split_equivalence: 187 passed
- 门禁 run-gate-tests.sh: 796 passed, 4 skipped（commit msg 说 800，实跑 796，全绿）

### 任务1 读写契约 SSOT
- AST 脚本提取渲染层 70 个顶层读取键 × 构建链 161 个写入键
- 8 个疑似孤儿键核实后：6 个误报/兼容代码（atr/chanlun/chan/execution=旧别名兜底；suggested_pct=decision_view 写；key_levels=structure_stage 写 levels 经 setdefault 进 report）
- `pool_count`/`pool_cap`（short_midline L1876-1877）：渲染层读但 build_report 生产链不写，仅 tests stub 塞；有 None 兜底不崩，属外部调用方注入的可选展示字段 → 需人工裁定

### 任务2 死代码（赋值后未引用）
- `_rr_chase_verdict`（L1536）：计算后未用，_rr_buy_verdict 在 L1602 用了 → 最可能是遗漏
- `key_levels`（L1732）：`r.get("key_levels")` 读出但后续未引用 → 疑似遗漏
- `far_sell`（L1510）、`_buy_hi_val`（L1862）、`daily_ruling`（L540）、`short`（L734）、`conflict`（L745）：均赋值后未引用 → 建议改
- `_ = stage`（assemble L300 / attach_stage_pack L81）：有意丢弃，非 bug

### 任务3 脆弱转换
- **short_midline L1430** `int(_msig.get("direction",0))`：潜伏雷，与 L1429 `_safe_int(_csig2...)` 不对称，commit 0c58ed6 修了 _chan_dir2 漏了 _mom_dir2 → 建议改
- report_builder L239/241 `float(bars[-5]["close"])`、attach_sync L36-41 `float(report["support"])` 等：潜伏雷（数据源一般干净，脏值会崩）→ 需人工裁定

### 任务4 Wyckoff 状态机（全绿）
- _PHASE_ORDER 11 phase，_detect_phase 产出的 11 字面量全部覆盖
- 全配对 121 组合不变量成立（同方向只加深/反方向允许翻转/none 保持/force_apply_none 生效）
- 专项单测 test_wyckoff_phase_transition.py 8 个 test_

### 任务5 缠论结构/链路不变量（全绿）
- classify_structure 枚举严格落在 {无结构/单边上涨/单边下跌/盘整/上涨趋势/下跌趋势}，无新主状态
- 趋势拓扑一致：classify_structure / _strict_up/down_trend_zones / _divergence_kind_for_zones 共用末两中枢语义
- build_segments 不变量全成立：3笔成段/三分型+第二类破坏共用尾部护栏/9笔1段/向上low跌破·向下high突破对称
- 方向单源：format_chanlun_theory_line 复用 resolve_chanlun_primary direction（卖点→买点→背驰→趋势兜底，正式买卖点优先于背驰）
- 文档-代码一致：formulas.md §3.6/§3.7/§4.3/§9.4/§11A 阈值逐条对齐

### 必须再改清单
1. short_midline L1430 `_mom_dir2` 改走 `_safe_int`（与 L1429 对称，commit 0c58ed6 漏修）
2. short_midline L1536 `_rr_chase_verdict` / L1732 `key_levels` 等死变量清理或补用

## 写了状态语义保真审计 handoff
- 文件：`docs/plans/wyckoff-chan-state-audit-handoff.md`
- 用途：给换的查 Agent 接手，查「数据算对没 / 状态定对没」（前序只查了自洽性）
- 7 项查法 A-G：D=信号→phase映射（最高优先）/ E=持久化污染 / A=事件检测器保真 / B=中枢段数据 / C=volume单位 / F=黄金样本回放 / G=周线日线隔离
- 核心认知写入 handoff：自洽 ≠ 正确；前序查「不会自相矛盾」，本 handoff 查「判的是不是真相」
- 边界：纯只读审计，不改 .py，不动 fusion/decision_view/出手/池

## 接手执行 wyckoff-chan-state-audit-handoff（读 Agent 审计）

按 `docs/plans/wyckoff-chan-state-audit-handoff.md` 跑完 7 项查法 D/E/A/B/C/F/G（只读，未改任何 .py）。

### 验证结果（自写只读脚本，/tmp 未入仓）
- 查法 D 信号→phase 映射：19/19 通过（五阶段叙事 if/elif 全部正确；premature/phase_a_failed/low_tr/P2 闸/簇兜底均工作）
- 查法 E 持久化：16/16（翻转及时/first_seen 重置/跨日确定性/symbol::timeframe 隔离全过；中线周线 use_persisted_phase=False 已确认→污染休眠）
- 查法 A 事件检测器：7/7（BC/Spring/SOS 合成触发正确、平稳序列不误触发、BC 量比 1.2×<1.5 不触发）
- 查法 B 中枢段：11/11（build_zones 绑实际极值、非法窗口丢弃、_merge_zones 仅合并真重叠+连续、MACD 面积坐标雷确认调用方传 cleaned）
- 查法 F 轨迹（合成）：10/10（积累/派发五阶段严格按序、反向不黏住）
- 查法 C volume 单位：清洁（检测器全用量比，手/股差异抵消，无 100× bug）
- 查法 G 周/日隔离：清洁（_phase_key=symbol::timeframe）
- 基线复跑：wyckoff+report 179 / chan 187 / 门禁 810 passed 4 skipped

### 4 项发现（均非必须改）
- FINDING-1 建议改(低)：tr_upper 读 signals、tr_lower 读 tr_ctx 来源不一致（wyckoff_phase.py:576-577 vs 596），生产两处都填故潜伏
- FINDING-2 建议改(中)：markup 分支(L580)缺 _dist_cluster 簇兜底，与 accumulation_d(L619) 不对称，派发簇确认+Spring+SOS+突破仍判 markup
- FINDING-3 建议改(中)：use_persisted_phase 默认 True，wyckoff_core:1286 / review_core:350,602 / t0_run:195 日线路径依赖默认→日线 phase 被持久化（中线周线已显式 False）
- FINDING-4 需人工裁定：真实票 K 线目检需联网，离线未做（建议抽 3-5 只已知 BC/Spring/SOS 票核对）

### 输出
- 报告：`docs/plans/wyckoff-chan-state-audit-report.md`
- 结论：状态机语义基本正确，73 项自写断言全绿 + 基线全绿；无「必须改」项

### FINDING-3 修正（用户追问收益/风险后澄清）
- 原报告「建议默认改 False」是**错的/危险**的。日线 `use_persisted_phase=True` 是有意为之的稳健层：让日线 phase 跨日连续、翻转需确认，避免单日信号抖动导致 phase 跳变（即 E 查法验证的持久化机制）。中线周线用 False 是另一语义（周线战略依据不被单周污染）。
- 正确处置：**不要动默认值**。最多把日线调用点 `wyckoff_core.py:1286` 显式写上 `use_persisted_phase=True`（消除隐式依赖，行为不变，风险≈0）。
- 教训：审计报告的「建议改」要区分「真实产品收益」vs「代码卫生」；FINDING-3 属后者且改错方向有害。

### 用户决策风格（记）
- 动手前习惯性先问「改了有什么收益 / 有什么风险」——倾向务实权衡，不盲目照审计报告清单改。

### 动手改 F1/F2/F3（本地 commit 27355a4，未 push）
- F2: wyckoff_phase.py L574-593 把 `_dist_cluster` 定义提前到 markup 段 + markup 进入条件加 `and not _dist_cluster`（与 accumulation_d 对称）；派发确认后 Spring+SOS 假突破不再误判主升
- F1: wyckoff_phase.py L576 markup 的 `tr_upper` 从 signals 改读 tr_ctx（与 markdown L596 对称统一来源）
- F3: wyckoff_core.py L1286 日线显式 `use_persisted_phase=True`（不动默认值——日线持久化是有意稳健层）
- 新增单测 `test_wyckoff_phase_dist_guard.py`（2 项，钉死 F2/F1）
- 回归：wyckoff+report 199 passed；门禁 810 passed/4 skipped；零回归
- 未改：默认值、fusion/decision_view/出手/池、F4 人工目检（留待联网裁定）

### 做 F4 真实票目检（腾讯日线，已执行）
- 12 只流动性样本跑 `wyckoff_analysis(use_persisted_phase=False)`，事件棒按价反定位 + 量价形状判读 + 4 张走势图（`/tmp/f4_report.html`）
- **SC 抛售高潮：保真良好**（7 只全落近 40 日低位+放量 1.25×–4.02×）
- **BC 买力高潮：过触发（FINDING-5，中）**——12 只触发 11 只；根因 `_detect_buying_climax`(L639-727) 实为「局部供应棒」启发式（量比 vs 近窗均量、近窗高位≥0.65）；假阳实例：隆基 BC@13.43 量仅中位 0.87×且不接近 40 日高、宁德 BC 距 40 日高差 13%。下游 phase 未误判（积累股仍 accumulation_a），非阻断。建议收紧紧窗高位判定/抬高量比基线/或显式语义，改前需确认 test_wyckoff_core L131-456 BC 用例不回归
- SOS：构造上恒命中末棒，本批票近期走强，未见误放
- 报告已加 §11 + §8 加 FINDING-5 行

### 审查纪律教训（建议补用户长期记忆）
- 「不一致 ≠ bug」：提「统一/改默认值」前必须先确认两侧是否各自有意为之。FINDING-3 即因把"日线默认 True vs 中线显式 False"当成待归一化不一致、未先验证两侧语义而给了错误建议（后自查纠正）。

### 动手改 FINDING-5（BC 过触发收紧，本地 commit 待提交）
- 优先级从「中/非阻断」上调「高」：BC 下游 `stage_stops.py:247` 直接触发减仓（非仅展示误导），过触发会误减仓
- config.py 新增 `WYCKOFF_BC_HIGH_POS_LOOKBACK=60`（高位窗 10→60）+ `WYCKOFF_BC_PRE_RISE_LOOKBACK=60` + `WYCKOFF_BC_PRE_RISE_PCT=0.15`，并导出
- wyckoff_events.py: `_is_bc_high_position` 默认长窗(60)；新增 `_bc_has_pre_rise`（BC 前 60 日须从低点抬升≥15%）；`_detect_buying_climax` 加前置涨幅闸
- 宁德 300750 纠正：原 F4 误判为假阳，按新语义 BC@428.9 有 31% 前置主升+71.8% 高位，**是真 BC**，应为 True
- 单测：5 个平线 BC 用例补前置主升；新增 `test_bc_rejected_no_pre_rise`+`test_bc_real_climax_with_pre_rise`；`test_are_after_bc` 回落棒量 4000→1500（避免其本身被判 BC）
- 真实数据 12 只：BC 11/12 → 7/12，消除 5 只反弹棒假阳，保留 7 只均核验真主升
- 回归：wyckoff+report+chan 382 passed；realstock_verification 真实票 BC=True 通过；门禁 812 passed/4 skipped
- 手递文档 `docs/plans/wyckoff-bc-overtrigger-fix-handoff.md`；报告 §12 记修复

### git 整理（用户要求「删分支 + 合并本地 8 commit」）
- 删已合并的 `codex/report-wyckoff-state-fixes`（本地+远端）；wyckoff 3 commit 已 fast-forward 合入 github/main(75c4ac5)
- 本地 main 另有 8 个未 push commit（含 1 merge `89ecdb0`）。`git cherry` 验证 7 个实质 commit 全未合入 github/main
- 用户选 rebase 线性整合：`git rebase github/main`（stash 暂存未提交改动 → rebase → 解 1 冲突 → stash pop）
- 冲突：short_midline.py 信号分歧段（`_csig2/_msig` direction 取法）HEAD(_safe_int 防御版) vs f922c20(int+_momentum_direction_int)；保留 HEAD `_safe_int` 版（与 _csig2 取法一致、防非 int，不 reintroduce 状态机 int 坑）
- 结果：7 个 commit 线性重放到 75c4ac5 之上，merge commit 消失；门禁 816 passed/4 skipped（rebase 带 f922c20 报告测试 +4）；`--force-with-lease` 推 75c4ac5..0b0c796
- 教训：rebase 改写历史前必 `git cherry` 确认未合入，避免重复 commit；冲突解择「保留已验证的防御版」

## 写了系统可观测性优化 handoff（工程优化方向）
- 文件：`docs/plans/system-observability-optimization-handoff.md`
- 用途：给做 Agent 接手，5 项工程优化（隐式状态地图/决策日志/语义回归/异常可见性/数据源溯源链）+ 2 简提（配置敏感性/跨周期压测）
- 核心思路：从「人找问题」变「问题找人」；决策日志是最大杠杆（做完自动化前序 handoff 的 D/E/F）
- 性质：可改代码（加 trace/_meta/测试），但不动 fusion/decision_view/出手/池语义；禁止一刀切删容错 except
- 用户偏好更新：审计/工程建议类回答不出图（已记 ~/.workbuddy/MEMORY.md；股票框架分析阶段图保留）

## 接手执行 system-observability-optimization-handoff（D1 调研 + D3 实现）

按 `docs/plans/system-observability-optimization-handoff.md` 落地。本 turn 完成 D1 阶段一（调研）+ D3（异常可见性实现）；D2/D4/D5 + D1 阶段二待续。

### D1 隐式状态地图（阶段一·调研，未改码）
- 产出 `docs/plans/system-observability-state-map.md`：18 项 `~/.trader` 落盘点，每项标风险。
- 🔴 标红（无版本号+无失效，阶段二必改）：`wyckoff_phase.json`(改阈值旧 phase 黏住)、`stage_state.json`(跨日黏住+旧格式兼容分支脆弱)。
- 🟢 已有防护作样板：`backtest_cache`(写前 >5× 中位数坏点断言)、`cache/daily`(`vol_unit="lot"` 自描述+旧缓存强制回源)、`signals.jsonl`(signal_id 迁移)。
- 🟡 可顺手补 `schema_version`：phase_a_anchor/fund_flow/chanlun_state/tick_cache/pool/holdings/calibrated_params/t0。

### D3 异常可见性（实现 + 单测，6 passed）
- 新增 `trader_shared/exception_tracker.py`：全局计数 registry（`record`/`collect`/`reset` + `suppress_and_count` 上下文），默认开计数、绝不抛出、不改控制流；`TRADER_EXCEPTION_TRACK=0` 可关。
- `report_builder.build_report`：开头 `_et_reset()` 做报告级隔离，结尾把 `report["_exception_summary"]`（类型+位置+计数）挂上并 `_logger.warning` 摘要——面板不渲染（私有键，short_midline 不读）。
- 已把 4 个代表性「逻辑兜底」except 改为计数（不改语义）：`decision_view.py`(3处) / `fusion_stage.py`(1) / `analysis/cards.py`(1)。数据层容错 except 一律保留，未一刀切删。
- 验收点：故意注入异常 → `_exception_summary` 可见（单测覆盖）；生产默认开计数、不抛出。
- 回归：wyckoff+report+新单测 187 passed（基线 179 + 6 新），零回归。

### 待续（未做）
- D2 决策 trace 旁路（wyckoff_phase._detect_phase/_transition_phase + fusion/cards + chan_core，默认关、零损耗）
- D4 语义级回归测（`tests/test_semantic_regression.py`，10+ 票 phase 轨迹快照）
- D5 数据源溯源 `_meta`（daily_source/daily_cached/vol_unit/5m_source/weekly_source/tushare_available/fetched_at）
- D1 阶段二：#1/#3 补 schema_version + 失效测

## 接手「决策正确性 5 项已登记问题」（核验后真状态）

用户补充 5 个"比可观测性更接地气、当下影响决策"的问题，要求加进 plan。先逐条 grep+读码核验现状——**发现 MEMORY 旧笔记大面积过时**。

### 核验结论（关键：旧账已修/变小）
- **#1 动量席 score=50 双关**：核心已修。`momentum_core.py:144-161` 数据不足返回 `score=None, direction="insufficient"`；`fusion_card_signals.py:158-170` 映射 insufficient→dir=0+reason="动量数据不足"；`fusion_core.py:361-367` climax 置 None。**残留**：`fusion_core.py:427-431` 加权段 insufficient 仍满权重贡献0，与真中性数学等价，未"降权/跳过"（用户要的 fusion 侧判缺位没落地）。修法=重归一化 chan/vpf 权重，须先建 D4 语义快照作安全网。
- **#2 北向假接入**：渲染层无"北向"行（非面板假行）；仅 `fusion_core.py:640-648` 做置信度微调（status=="正常"才生效）。真实风险=内部 no-op 诚实性，非可见误导。修法=get_northbound_flow 对全 NaN 诚实标 status!="正常"。
- **#3 test_contract 3 失败**：`pytest test_contract.py`=**37 passed/0 failed**；且 run-gate-tests.sh 的 TESTS 数组**不含**该文件。"3 失败"旧账不成立，无需处理。
- **#4 tushare 兜底诚实性**：前提已变。本机 `TUSHARE_TOKEN` 未设但 `tushare_client` 报 available=True（token 来自配置文件）；`get_provider()` 实测返回 `UnifiedProvider(backend="mootdx")`——**实际数据源是 mootdx（通达信直连），非 tushare 非 tencent**。用户"以为 tushare 实际 tencent"担忧与现状不符；透明度问题留给 D5 _meta。
- **#5 渲染死代码**：行号漂。`_mom_dir2` 漏 `_safe_int` 已修（short_midline.py:1533 已用 `_safe_int`）；`_rr_chase_verdict`(L1639)+其依赖 `_ratio_chase`(L1637) 确认零引用→删；`key_levels`(L1835) 零引用→删。`_rew_chase`(L1635)/`_rr_buy_verdict`(L1638) 有引用保留。渲染测试 19 passed 无回归。

### 交付
- handoff：`docs/plans/decision-correctness-handoff.md`（5 项核验后真状态+修复方案+依赖+禁止项）
- MEMORY.md 校正：#1 债笔记、#3 门禁笔记改为核验后真状态；加流程教训「MEMORY 技术债笔记会过时，改前先核验」。
- #5 死代码已清理（未提交）；#1/#2/#4 待做（#1 依赖 D4）。

### 流程教训（重要）
- MEMORY 里"技术债/失败"笔记会过时。本次 #1/#3/#5 实际已修或变小——**改代码/提方案前先 grep+读码核验现状，勿照旧笔记盲改**。这与用户重视的"诚实性"一致。

## 第四轮审计 + handoff 合并修正
- 初写 `known-issues-fix-handoff.md`，核验后发现与已存在的 `decision-correctness-handoff.md` 重复且含过时断言（_mom_dir2 已修 L1533 非"漏修"；test_contract 37 passed 非"已删"——它在 01-功能包路径；tushare 实际 mootdx）
- 修正：删除 `known-issues-fix-handoff.md`，把独有项（S1 周线口径/S2 regime准确率/S3 mock耦合/S4 skill双安装位/S5 大文件拆分）合并进 `decision-correctness-handoff.md` 末尾「补充项」章节
- 教训三次印证：自己的审计笔记（L1430 _mom_dir2 漏修）也会过时——rebase 后行号漂到 L1533 且已修；照自己旧审计写 handoff 同样危险，必须当轮重新核验

## #1 动量席 insufficient 重归一化 + D4 语义回归（已落地）
- 用户说 "ok" → 按推荐顺序先做 D4 安全网再改 #1。
- **D4 安全网**：`tests/fusion_regression_helpers.py`（9 场景合成三卡，零网络，run_scenario 调 merge_decisions 抓指纹）+ `scripts/dev_capture_fusion.py`（抓/ diff 基线）+ `tests/fixtures/fusion_semantic_baseline(_pre).json`（改前/改后锚）。
- **#1 修复**（`fusion_core.merge_decisions`）：
  - 检测 `mom_insufficient = momentum_result["momentum"]["direction"]=="insufficient"`（与 L367 climax 口径一致）。
  - 分歧列表 `_present_signals` 排除 insufficient 动量；加权段剥离动量权重、按原比例重归一化 chan/vpf 到 1.0；`compute_confidence` 仅用活跃席权重；`weights_used` 回写实际权重。
  - 禁止改回 insufficient=50。
- **验证 diff（改前→改后）**：`mom_insufficient_chan_vpf_bull` 0.27/增持/conf0.39 → **0.491/半仓试/conf0.90**；`mom_insufficient_chan_vpf_bear` -0.436→-0.545；`mom_insufficient_weak_regime` 0.41→0.482；`mom_real_neutral_chan_bull` **不变**（0.27）；分歧场景 `chan_bear_vpf_bull` ws≈0 仍保留（不强行偏多）。
- **测试**：`tests/test_semantic_fusion_regression.py`（基线稳定 + 不变量：insufficient 权重 stripped=0 / 真中性保留 / insufficient 主导≥中性）3 passed；门禁 816 passed/4 skipped。
- **真实票影响**：几乎为零——normal 上市票 bars>>30，momentum 不会 insufficient，故生产路径不动；仅 <30 根动量数据的极少数边缘票受影响（正是语义正确的方向）。
- 改动未提交/未 push（默认纪律）。

## #4/D5 数据源溯源 _meta（已落地）
- 用户说 "#4/D5 做了" → 在报告加 `report["_meta"]`（D5 / #4 透明度），不影响面板渲染。
- 实现：`light_data.build_source_meta(snapshot, provider)` 纯函数（零网络、可测）+ `report_builder.build_report` 在 return 前挂 `_meta`。
- `daily_cached` 真命中检测：新增 `_mark_cached(bars)`（浅拷贝首根打 `cached=True`，不污染缓存存储），在 `fetch_qfq_daily` 三处缓存命中返回点（熔断/文件缓存/URL 缓存）调用；网络路径不打。
- 字段：`daily_source`/`5m_source`/`weekly_source`/`vol_unit` 直接读 bar 自带 `data_source`/`vol_unit`（honest，无 race）；`provider_backend`=名义选源；`tushare_available`；`fetched_at`。
- **实测 600519**：`provider_backend="mootdx"` 但 `daily_source="tencent-http"`（daily_cached=true）——证明名义选源≠实际抓取源，_meta 把真相都暴露（正面回答 #4 担忧）；`weekly_source="daily_aggregate"`、`vol_unit="lot"`。
- 测试：`tests/test_source_meta.py`（5 passed，合同锁 _mark_cached 不污染入参 + build_source_meta 各分支含 daily_cached 命中/未命中/空 bar）；门禁 816 passed/4 skipped。
- 改动未提交/未 push。
