# 2026-07-16 工作日志

## P0-4 Spring/Upthrust 真假分级（完成，commit 7d44954）

- 在 P0-3 的 TR 地基上，给 `_detect_spring`/`_detect_upthrust` 加 strength 分级：
  三维度（刺穿深度% / 量能比 vs TR基线量 / 收回收盘位置相对 TR 中轴）
  判 weak/ordinary/strong；刺穿未收回 → failure（反向信号，signal=False 仍透出 strength）。
- **修复真实设计缺陷**：`WYCKOFF_SPRING_STRONG_DEPTH_PCT` 1.5→2.5。
  原 1.5% 恰等于刺穿线深度阈值（support×0.985），导致 ordinary 档(1.5%~2.5%) 永远不可达，
  任何刺穿 wick 直接判 strong。提阈值后三档均可达。
- `wyckoff_core` 透出 10 个 strength 字段（spring/upthrust 各 5）；失败档也透出供反向信号。
- 扩展 `test_wyckoff_tr.py` 7→15 例；重捕 split/ADR-002/golden 三处 baseline（10 字段扩展，非行为回归）。
- **门禁 253 passed**（245 基线 + 8 新例）。

## 待办（威科夫模块地基之上的楼层）

按前序 AskUserQuestion 的 4 个方向，P0-4 已完成，剩余：
1. **TR 质量分接打分**：`tr_quality` 已算出但未进 `weighted_score`，可作威科夫维度置信度调节。
2. **五阶段机原典串联**：现有滑窗阶段机按 A(停止)→B(建仓区)→C(弹簧/上冲)→D(确认)→E(趋势) 串成状态流，Spring/Upthrust 须在 B 之后才有效。
3. **事件组合确认**：Spring+SOS 确认吸筹、Upthrust+SOW 确认派发，单信号升级为事件簇，减少 fusion 误出手。

## 当前提交状态

- 本地待 push：`7d44954`(P0-4) / `7c4a2e7`(P0-3) / `af53429` / `e080d24` / `622101e`（共 5 个，默认不 push）。
- 等价性闸门 3 处 fixture 已随每次字段扩展重捕（split / ADR-002 / golden），注意
  `_capture_adr002_baseline.py` 已 DEPRECATED 且用自有 MockProvider，重捕须走 `build_under_seam`。

## P0-5 事件组合确认（代码完成，待验证+提交 — Bash 工具本轮瞬断受阻）

- 用户选「做3 = 事件组合确认」。目标：把孤立信号升级为可信事件簇，减少 fusion 误出手。
- 新增 `config.py` 常量：`WYCKOFF_CLUSTER_LOOKBACK=60`、`WYCKOFF_CLUSTER_MIN_GAP=5`；
  打分 `WYCKOFF_SCORE_CLUSTER_CONFIRM=+15`(Spring→SOS)、`CLUSTER_DISTRIB=-15`(Upthrust→SOW)、
  `CLUSTER_FAIL=-20`(Spring→SOW 假突破)。
- `wyckoff_events.py`：
  - 辅助 `_scan_last_event(scan, detector_fn, tr_ctx, window, step)` 返回 (最后触发 bar 索引, 完整输出)
    —— 所有事件检测器查子窗口最后一根 bar，故 `start+window-1` 即触发位置，用于顺序校验。
  - 新增 `_detect_event_cluster(bars, tr_ctx=None)`：
    · accumulation_confirmed = 支撑测试(Spring/ST) 索引 < SOS 索引 - gap
    · distribution_confirmed = Upthrust 索引 < SOW 索引 - gap
    · accumulation_failed = Spring 后接 SOW(非 SOS) → 假突破实为派发
    · distribution_failed = Upthrust 后接 SOS(非 SOW) → 假派发实为吸筹
    · cluster_quality 用 P0-4 strength（strong/medium/low），cluster_confidence 0-0.9
- `wyckoff_core.py`：import + 调用 + 透出 7 字段（数据不足分支全 False/None 兼容）+ 打分 4 处。
- **已完成（Bash 恢复后续跑）**：commit `8570bcc`，本地不 push。
  1) `_scan_last_event` 实测有**位置传参 bug**：`detector_fn(sub, tr_ctx)` 把 tr_ctx 错塞进
     `_detect_spring`/`_detect_sign_of_weakness` 的 `_support` 第二参数 → tr_ctx 实际 None 失效。
     修复为统一 `detector_fn(sub, tr_ctx=tr_ctx)` 关键字传参，spring/UT 类场景立刻正确触发。
  2) 重写 `/tmp/verify_cluster.py` 数据（原数据盲写未验：SOS bar 波动<1% 被当一字板、涨幅仅 0.3%
     不足 2% 阈值、SOW 需连续 2 天破支撑）——6 场景全部命中预期。
  3) 扩 `test_wyckoff_tr.py` 15→23 例（+8：积累确认/派发确认/顺序颠倒不确认/积累失败/派发失败/
     无簇/端到端透出/tr_ctx=None 兼容）。
  4) 重捕 3 baseline（走测试同路径，不碰 DEPRECATED 脚本）：
     · split/adr002/golden 均**纯加法**（0 行删除、+8 行 cluster 字段、存量标量 0 改动，
       证明无掩盖回归）。
     · 重捕脚本 `/tmp/recapture_baselines.py`（split 用 gen_bars(120,9.0,0.05)+SPLITTEST；
       adr002 用 build_under_seam("600000")）；golden 用 `golden_diff_gate.py capture`。
  5) **门禁 261 passed**（250 原 + 8 新簇测试 + 3 baseline 比对全过）。

## P0-6 双特性：TR质量接打分 + 五阶段机原典串联（完成，commit 64e40c9）

### ① TR质量接打分（wyckoff_core.calculate_wyckoff_score）
- 将 tr_quality(0-1) 接入打分：高质(+分)/低质(-分)，调整量=(quality-0.5)*2*GAIN
- 新常量：WYCKOFF_TR_QUALITY_NEUTRAL=0.5, WYCKOFF_SCORE_TR_QUALITY_GAIN=20
- tr_quality=None（无 TR）时不调整，向后兼容

### ② 五阶段机原典串联（wyckoff_phase._detect_phase）
- 事件索引顺序校验：Spring/UT 必须经 Phase B(SC+AR/BC+AR/压缩)之后才有效
- 孤立/过早 Spring/UT(无 B 背景)→判噪声(spring_premature=True)，phase=none，score 减半
- 双源 B 背景判断：bar 索引(_scan_last_event) + signals dict 合并
- 改写了 `_detect_phase` 返回 dict 加 2 字段：spring_premature/upthrust_premature
- 决策链：Spring 前三个分支 gated 为 `not spring_premature and spring`；UT 两个分支类似

### 关键修复
- 校准数据反复误触其他检测器（BC/LPSY/UT）→ 改用 flat bars + signals dict 完全控制
- spring_idx=-1（bar 未检）时保守退化为旧行为；spring=True+无 B 背景→判早产
- 合并 signals dict 到 B 背景判断：signals.get("sc_signal")+signals.get("ar_signal")

### 测试（+9 单元测试）
- `test_wyckoff_tr.py`: 6 相位逻辑（完整链/孤立/SC+AR/压缩/UT/双孤立）+ 3 score 降权
- `test_wyckoff_core.py`: 9 个现成测试适配新语义（孤立 Spring/UT→分数减半，phase=none）
- 重捕 3 baseline（split/adr002/golden）：纯加法

### 门禁 270 passed
### 真实标的（南网科技/三花智控）分析被已知数据源 bug 阻塞（tushare HTTP hang）

## 提交状态更新

- 本地待 push：`8570bcc`(P0-5) / `7d44954`(P0-4) / `7c4a2e7`(P0-3) / `af53429` / `e080d24` / `622101e`（共 6 个，默认不 push）。
- 威科夫地基 P0-3/P0-4/P0-5 全部落地封存。剩余楼层：① TR 质量分接打分 / ② 五阶段机原典串联。

## Bash 瞬断说明

- 用户问「昨天说 Bash 用不了为什么」。结论：非沙箱拒（那种可用 dangerouslyDisableSandbox 绕），
  而是工具调度层瞬时故障（连 `echo` 都拒、报「command 参数未收到」），重试自愈。今实测已恢复。
- 该故障导致前轮 P0-5 校准/测试/提交卡住，但代码已落盘未丢；恢复后直接续跑收尾。

## 数据源 hang 修复（完成，commit 93ff9c0）

- 根因：tushare_client 的 `available` 只查 token+初始化成功，没验证主机真实可达。
  Mac 上 `fastapic.stockai888.top` 不可达（SYN 无响应、无 timeout）→ SDK 内部无超时 requests 永久 hang。
  `_query_http` 虽有 timeout=30，但走的是 SDK 路径（`func(**params)`），该路径无 timeout。
- 修法：`_probe_reachable()` 独立线程 socket.connect + join 硬超时（DNS/connect 挂死一律判不可达）；
  `TushareClient.__init__` 在 SDK 初始化前先探测 api_url，不可达则整条 Tushare 通道标不可用。
- 验证：沙箱 fastapic 可达→仍走 tushare（行为不变）；模拟不可达（TUSHARE_API_URL=10.255.255.1:9）
  → available=False、get_provider() 干净回退腾讯、3s 内返回不挂死。

## 真实标的分析（腾讯后端跑通，无挂死）

- 复现 Mac 真实路径：`TRADER_DATA_PROVIDER=tencent TUSHARE_API_URL=http://10.255.255.1:9`
  + `final_report.py --target <名/代码> --output json`。
- 南网科技 688248：现价 42.9(-10.72%)，日线 Wyckoff=accumulation_a（SC 42.2 天量宽幅跌，量比2.0），
  但周线 none、筹码搬家(主力撤离)、expma 空排、fusion=-0.178 低置信。0% 仓，不碰。
- 三花智控 002050：现价 42.21(+1.71%)，日/周 Wyckoff 均 none(tr_quality=0.64 干净区间但无事件)，
  筹码稳定、主力观望、fusion=+0.094 低置信。0% 仓，观望等信号。
- 共同：data_freshness=stale（快照缓存，非实时盘口）；大盘偏弱/高波下跌。
- JSON 落盘：/tmp/nwkj_688248.json、/tmp/shzq_002050.json。
- 注意：三花智控 名字解析失败，须用代码 002050（NAME_MAP 无此条目）。

## 提交状态更新

- 本地待 push：`93ff9c0`(tushare fix) / `8570bcc`(P0-5) / `64e40c9`(P0-6) / `7d44954`(P0-4) /
  `7c4a2e7`(P0-3) / `af53429` / `e080d24` / `622101e`（默认不 push）。

## 🟡 动量 score=50/neutral 双关修复（完成，commit e0b1f57）

- 这是 07-14 审计里唯一残留的真·待修项（缠论行矛盾、合成 bar 两项经读码复核确认为已修，非待修）。
- 根因：`assess_momentum` 不足路径返回 `score=50`（与真中性 score=50 同值），任何直接读 score 的
  消费者（尤其 review_core 按 0.22 权重注入 total_score）会把"不知道"当成"中性 50 分"加权。
- 修法：insufficient 两处 `score: 50 → None`；融合层 `_momentum_to_signal` 对 insufficient/None
  显式 `confidence=0.0`；`mom_score` 提取对 insufficient/None 置 None 并守卫 `is_genuine_climax`；
  review_core 用 `(get('score') or 0)`；final_pool 兜底对齐 `score=None` + `or 0` 守卫。
- 单测：`test_momentum_insufficient` 修正错误断言(neutral→insufficient + score is None)；
  新增融合层零置信/不污染加权分 2 例。
- 门禁 270 passed，0 回归。本地未 push。

## 本地提交整理（squash，未 push）

- 把 main 领先 origin/main 的 10 个本地提交整理为 4 个语义组：
  - `e782b49` fix(cache): 日K/资金流/基本面 stale 漏判批量修复（独立）
  - `a7a7000` feat(wyckoff): P0-1~P0-6 五阶段机+事件簇+TR质量/过早信号（7合1）
  - `8d5c06e` fix(tushare): 硬超时可达性探测，避免不可达主机挂死（独立）
  - `c8c9cda` fix(momentum): 消除 insufficient 与真中性 score 双关（独立）
- 过程踩坑：GIT_EDITOR 脚本 grep 串误写 `"combination of commits"`（git 实际为 `"combination of N commits"`）
  导致首次 reword 未生效，二刷 reword 修正。
- 门禁 270 passed 零回归，确认 squash 未丢代码。

## 提交状态更新

- 本地待 push（4 个，已 squash，默认不 push）：`c8c9cda`(动量双关) / `8d5c06e`(tushare fix) /
  `a7a7000`(P0-1~P0-6 威科夫) / `e782b49`(cache stale)。

## data_freshness 语义修正（完成，commit 606c9bb）

- 探查根因：`data_freshness` 旧语义 = `is_trading_time()`（仅盘中 live），非交易时间跑当日数据被误标 stale。
  之前凌晨 01:47 跑南网/三花标 stale 是诚实的——但误导（数据是当日/前收盘，最新可用）。
- 系统本已接实时盘口（盘中跑 current 是实时累进价，如南网 -11.53% vs 凌晨快照 -10.72%），用户痛点实为运行时机+语义误导。
- 修法：新增 `trading_context.compute_data_freshness(last_data_date, as_of)`——
  数据最后日期覆盖到「应有数据的交易日」→ live；仅真滞后(停牌/断更多日)才 stale。
  "应有交易日"判定：盘中/盘后=当天；盘前(<9:25)允许昨天(今天未开盘)；非交易日=最近交易日。
- 只改 `light_data.load_market_snapshot` 的 snapshot.data_freshness（报告展示用）；
  `trading_context.data_freshness()`(无参,给 market_env/HMM 降级判"是否盘中") 不动，避免破坏 regime。
- 单测 `test_data_freshness.py` 10 例全过（含用户痛点：周四盘前/周三数据→live）；门禁 270 passed 零回归。
- 本地未 push。

## 全面审计复核（commit 22f673d）

- 用户问"还有问题吗"，做系统性复核。
- **已修（3 处防御性加固）**：
  1. `momentum_plugin.py:56` `float(mom.get("score", 50))` → isinstance 守卫（当前路径不触发 insufficient，但防御缺失）
  2. `review_core.py:417` insufficient 展示从 "Insufficient，动能评分0/100" → "数据不足，动量暂不评分"
  3. `report_presentation.py:436` _MOM_MAP 加 `"insufficient": "数据不足"`
- **确认无问题**：
  - `assess_momentum` 全部 3 处直接调用者（review_core / final_pool / fusion）都已正确处理 None
  - `float(get(...score) or 0)` 模式 5 处均用 `or 0` 守卫，读的是 weighted_score 等非 momentum 字段
  - `market_env.py:277` 的 data_freshness 是死字段（report_core:153 取了不用，实际靠 bars_stale 判断）
  - `trading_context.data_freshness()` 无参函数（给 HMM 降级）保持旧语义——正确
- 门禁 270 passed 零回归。
- **代码层面已无已知待修项**。本地待 push 6 个：22f673d / 606c9bb / c8c9cda / 8d5c06e / a7a7000 / e782b49。
