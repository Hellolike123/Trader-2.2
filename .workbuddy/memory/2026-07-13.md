# 2026-07-13

## Trader 架构评审与重构计划讨论
- 用户给了两份文件:`architecture-review.md`(架构诊断,含 P0/P1/P2 + ADR-001~004)与 `task-stabilize.md`(止血第一步,纯清理,显式排除架构重构)。
- 四步重构计划(来自 architecture-review.md 演进顺序):
  1. 止血/清理 = ADR-004(删 34 stub + 双 config 入口 + 死集成 self_calibration)
  2. P0 收编 = ADR-001(把 pipeline/signal_tracker/market_env/calibrator 收编进 trader_shared,删 __init__.py 的 importlib 向上依赖,使其成为真库)
  3. 拆 monolith + 统一渲染 = ADR-003(run_analysis 3240行 / final_pool 2403行 混合层;三套渲染器漂移)
  4. 走 PluginRegistry 组合点 = ADR-002(build_report 改走 get_registry().analyze_all(),解决主路径绕过组合点)
  - 另需补测试基建(conftest/pytest/CI + render_single 单测),文档把它放第 5 步(最后)。
- 我的架构师判断(与文档不同处已标注):
  - ADR-001 选 A(收编)还是 B(DI)取决于前提:用户目标是"契约输出给别的 agent"→ 应做真库,选 A。
  - P1/P2 高风险(动主路径/大文件),必须在测试基建就位后做。我建议把测试基建从文档第5步提前到 P0 之后、P1/P2 之前。
  - P1 是成本/可读性问题非结构违规,优先级最低。
- **用户反馈(重要,复盘教训)**:作为架构师要直接 engage 架构层(ADR/P0-P2 的取舍),不要陷在"改动落仓库还是部署副本"的打包机制争论里。前面几轮我偏到部署层,用户明确点出"没觉得你在优化架构"。
- 已核实:`pack_all.py` 的 `_STUB_MODULES`(167-189行)每次打包自动生成 34 个 re-export stub;四个 skill 的 scripts 均无裸依赖,删生成逻辑安全。
- **更正(重要)**:task 文件称"6 处裸自引用"在仓库里实为 6 个模块 docstring 里的用法示例(非运行时代码);运行时导入本就用 `trader_shared.X`/`.X`,根本不依赖 stub。删 stub 比原 task 预想的更低风险。
- **已执行(方案 A,commit 0b48fdc,基于 f086ec3,未推送)**:
  - 仓库改 8 文件:5 个模块 docstring 示例改 `trader_shared.X`;fusion_core 删 self_calibration 死集成(T2)+ 修 docstring 错路径(T3);test_compare_signals 删未用的 `import candidate_core`;pack_all 删 `_STUB_MODULES` 生成块。
  - 重打包 → hermes(干净覆盖,旧 stub 一并删);同步 hermes → workbuddy(先备份 /tmp/trader_*_bak_*)。两份均 py_compile OK + 全量 19 模块导入冒烟 OK,无 stub 依赖。
  - 验证遗留:仓库内 pytest 因 PYTHONPATH 顺序使 `config` 解析到 trader_shared/config.py 而收集失败(final_pool `from config import ADMISSION_SCORE_EXECUTE`);部署副本内 pytest 因取数被沙箱 SIGKILL(137)。两者均为环境/网络限制,非本次改动引入。
- 下一步待用户拍板:ADR-001(P0 收编)选 A/B,以及是否提前补测试基建。
- **已切重构分支**:`refactor/trader-architecture`(基于 0b48fdc,main 未动)。后续 ADR-001/002/003 在此分支逐步落地,每 ADR 一 commit,可整体丢弃或单 commit revert。`main` 保持可发布。默认不 push 分支。
- **ADR-001 调研完成(实测,已落成 docs/ADR-001-colocate-shared-library.md)**:
  - 向上依赖有 3 处真代码源,非文档说的 1 处:`__init__.py:79-205` 懒加载 + `structure_core.py:41` 直接 `from self_calibration import` + `cache_utils.py:434-435` `sys.path` hack + `from market_env import`。
  - 待收编模块实为 **5 个**(文档说 4 个,漏 self_calibration):pipeline/signal_tracker/market_env/calibrator/self_calibration。
  - **实测结论:选 A(收编)是必选项**——选 B(显式 DI)管不到 structure_core/cache_utils 的直接裸 import,不彻底。
  - 影响面:~7 处 import 改写 + 5 大文件 git mv + __init__.py 顶层 re-export 保兼容;~15 个测试裸 import 暂靠保留 scripts path 注入不断。
  - 风险:零测试基建下操作有回归风险,建议**先补最小测试基建再执行本 ADR**。
  - 用户授权"自动执行后几步(goat模式/无人值守)":先补最小测试基建,再 ADR-001 收编。
- **已自动执行(深夜无人值守,2026-07-13 ~02:05)**:
  - 最小测试基建:`02-共享模块-shared/tests/conftest.py`(注入共享根到 sys.path) + `test_imports_smoke.py`(纯导入冒烟,无网络,覆盖 22 个核心子模块 + 公开 API write_stock/log/assess/run)。commit b3d9e4c。
  - ADR-001 收编:`git mv` 5 模块(pipeline/signal_tracker/market_env/calibrator/self_calibration)进 trader_shared;__init__.py 删 importlib 懒加载改静态 re-export + 裸名别名过渡层;改内部/运行/脚本裸 import 为 trader_shared.X(market_env/structure_core/cache_utils/calibrator/signal_migration_tool/check_all/review/final_tracker)。commit 2bc3493(amend 后)。
  - **实测新增修正(文档未列)**:`calibrator.py:33` 有 `from trader_shared import assess` 内部循环依赖 → 改 `from trader_shared.market_env import assess`;`fusion_core.py:19` 是裸引用 docstring(已改文档示例);`scripts/check_all.py:63` 也需改。
  - 验证:仓库 pytest `test_imports_smoke` **2 passed in 0.72s**(首个真绿单测);重打包 → hermes + workbuddy 两份副本 **19 模块导入冒烟全 OK**,无 stub 依赖,trader_shared 顶层可导入收编模块 + 公开 API 完整。
  - **trader_shared 现为可独立 import 的真库,无向上依赖 scripts/**(ADR-001 达成,P0 闭环)。
  - **未做**:ADR-002(走 PluginRegistry 动主路径)、ADR-003(拆 monolith)——按边界留用户醒来拍板,动主路径/拆大文件盲跑回归风险高,不在无人值守范围。
  - 分支 `refactor/trader-architecture` 未 push;`main` 停在 0b48fdc 未动。

## 凌晨续做(2026-07-13 ~02:30–02:50,用户授权"最高权限+明早验收+每步验证")
- **新建 golden 行为回归测试** `02-共享模块-shared/tests/test_build_report_golden.py`:mock provider 全离线 + 包裹 5 个策略函数(chan/wyck 日线+中线 + momentum)统计调用次数 + 断言 report 形状/weighted_score 范围/中线 key/5 策略全调用。单次 build_report + faulthandler 90s 熔断。基线 1 passed(37s,重计算使然)。commit f5ebb29。
- **ADR-003 执行(commit 201ea0a)**:
  - 关键实测:`build_report` 不只调分析 helper,还**直接调渲染/信号 helper**(volume_observation/_get_buy_label/action_text_for_scene/signal_max_total_pct/signal_risk_flags 等)——与展示层未分层。第一轮只搬 build_report 触发 `NameError: volume_observation`,golden 当场抓到。
  - 修正策略:把**原 run_analysis.py 除 main/parse_args(CLI 入口)外的全部逻辑**整体切到 `trader_shared/report_builder.py`(2881 行,逐行重定位零改写);`run_analysis.py` 退化为 207 行 CLI 壳(头部 import + 再导出 + main/parse_args)。切片脚本 `scripts/_extract_report_builder.py` 可复现。
  - 验证:py_compile OK;两端部署副本 **20 模块(含 report_builder)导入冒烟全 OK**;golden 1 passed(10.87s)→ 行为零回归。重打包装 hermes + 同步 workbuddy 完成。
- **ADR-002 推迟(文档化,未实现)** `docs/ADR-002-route-via-plugin-registry.md`:
  - 两陷阱:① PluginRegistry.analyze_all 无中线概念,天真路由会丢 midline(可被 golden 抓);② `ChanlunPlugin.analyze` 调 chanlun_strategy **不传 weekly_bars**,而 build_report 日线调用传 weekly_bars → 路由后日线 chan 结果**静默漂移**,golden 只有范围断言抓不到。
  - 因 golden 无法验证日线 chan 逐字段等价,且用户授权含"无法验证需反馈"条款,故推迟。安全路径:扩展插件接口透传 weekly_bars + 加中线插件 + build_report 走 analyze_all + **增加前后报告逐字段 diff** 作等价性闸门。
- **当前分支状态**:`refactor/trader-architecture` 领先 main 5 commit:0b48fdc(ADR-004)/b3d9e4c(测试基建)/2bc3493(ADR-001)/f5ebb29(golden)/201ea0a(ADR-003)。均未 push。`main` 停在 f086ec3 未动。
- **待用户明早验收/拍板**:① 是否 push 分支;② ADR-002 是否按文档安全路径实现(需先扩插件接口+加逐字段等价验证);③ report_builder 2881 行是否进一步拆 domain/presentation 两层(ADR-003 文档已建议为后续)。

## 用户要求 push 分支(2026-07-13 ~02:56)
- **已 push** `refactor/trader-architecture` → `origin/refactor/trader-architecture`(=201ea0a),设 upstream tracking。Gitee 仅打印 PR 链接,**未建 PR、未合并 main**。
- `origin/main` 仍 = `be0f177`(未动)。本地 main 在 `0b48fdc`(领先 origin/main 2 commit,含 ADR-004),但其提交已在分支历史内可溯,无损。用户明确要求"push 到分支,不要合并到 main"。

## ADR-002 落地(2026-07-13 ~03:40,用户"先做adr002把")
- **目标**:`build_report` 经 `PluginRegistry.analyze_all()` 组合分析,使插件成为真实组合点(原直调 5 策略函数绕过组合点)。
- **两个陷阱均堵住**:
  - 陷阱 #1(中线丢失):`analyze_all` 增 `midline=True` 分支,仅 build_report 传 True 时算 `chanlun_strategy_midline`/`wyckoff_strategy_midline` 挂到 `chanlun_midline`/`wyckoff_midline` 键。**不注册全局 midline 插件**——避免 fusion_core/final_pool/review 的 analyze_all 调用被波及(最小爆炸半径,它们不传 midline)。
  - 陷阱 #2(日线 chan 静默漂移):`IndicatorPlugin.analyze` + 5 插件全增 `weekly_bars` 形参;`analyze_all` 透传 weekly_bars 给 ChanlunPlugin→`chanlun_strategy(weekly_bars=...)`。fusion_core 调 analyze_all 不传 weekly_bars→None,与其历史行为一致,向后兼容。
  - **双重 nudge 去重**:analyze_all 内 MomentumPlugin 已做 Supertrend nudge,build_report 移除原重复 `apply_supertrend_nudge`,否则动量被微调两次 → weighted_score 漂移(陷阱 #2 的另一种形态)。
- **等价性闸门(关键)**:`scripts/_capture_adr002_baseline.py` 捕改前精确字段(weighted_score/confidence/action/disagreement/双 midline 字典)→ `tests/fixtures/report_baseline.json`;`tests/test_build_report_adr002_equivalence.py` 路由后逐字段递归 diff(float 容差 1e-6)。这恰好堵 golden 范围断言抓不到的日线 chan 等价退化。
- **验证全绿**:imports smoke + golden(5 策略调用计数+中线 key 存活) + 等价性闸门;`weighted_score=0.034` 与基线一致;既有 analyze_all 相关测试 `test_arch_refactoring.py`+`test_indicator_enhancements.py` **39 passed**;8 文件 py_compile OK。
- **commit** `b7eb2e4`(12 文件,+800/−49),**已 push** 分支(远程=本地=b7eb2e4),未合并 main。
- **有意未做**:部署副本(~/.hermes/~/.workbuddy)**未**重打包——分支仍重构中(report_builder 2881 行待拆 domain/presentation),避免把半成品推入运行中 agent;待 promote 时由 pack_all 从仓库统一再生。
- ADR-002 文档 Deferred→Accepted,附落地记录+验证结果+未做项。

## ADR-003b 落地:report_builder 拆 domain/presentation(2026-07-13 ~11:30,用户"继续")
- **目标**:把 `report_builder.py`(2870行)进一步拆为领域层 `report_builder.py` + 展示层 `report_presentation.py`。
- **用 AST 精确提取函数体**(ast.get_source_segment 按函数名分类),**不用行号切片**——首版行号区间法因 build_report 实际止于 1617 而非预期、中间带混合而翻车,改用 AST 稳健。
- **分类**:
  - 领域(report_builder.py, ~1635行):`build_report`/`_degraded_quote_report`/`determine_stage`/`structure_replay`/`sync_report_with_data`/`_calc_volume_ratio_from_bars` + `_logger`/`SCRIPT_DIR`。
  - 展示(report_presentation.py, ~1304行):`render_markdown` + 全部视图/格式 helper + 两个**仅被展示调用**的 fetcher `_get_kelly_data`/`_get_major_stage`(不被 build_report 调用,可安全搬) + 模块常量 `_kelly_cache`/`CONTRACT_VERSION`/`_SIGNAL_TYPE_LABELS`。
- **桥接**:`report_builder.py` 底部 `from .report_presentation import (24名)` 同时承担 re-export,使 `run_analysis.py` 31 名 import 零改动。依赖方向严格 builder→presentation,无循环导入。
- **⚠️ 关键命名陷阱(永久记)**:仓库已有**被追踪的** `trader_shared/report_renderer/` **包**(另一套渲染实验:render_single/render_short_midline/render_pool_summary/render_backtest,`01-功能包-packages/trader/tests/test_report_renderer.py` 在用)。与本次展示层**同名冲突**——同名目录抢先被当包,导致 `from .report_renderer import ...` → `ImportError`。**新模块命名 `report_presentation.py` 避开冲突**,旧 `report_renderer/` 包保持原样不动。以后任何新模块不要叫 `report_renderer`(已被占)。
- **等价性闸门**:`scripts/_render_eq_capture.py` 全离线确定性桩(覆盖 6 网络泄漏点:set_provider/TencentFetcher/get_env_for_skill/fetch_fund_flow_cached/tushare_client.get_client/chip_data.get_cyq_perf,日期掩码)。分裂前(2870)与分裂后(1635+1304)渲染输出 **md5 完全一致** → 行为保持。`tests/test_report_render_equivalence.py` 守门。
- **测试隔离 bug 修复(重要)**:桩内全局改写首版用裸赋值 `_fetchers.TencentFetcher = MockFetcher`(不还原),导致排在后面的 `test_arch_refactoring` 两个 fetcher 测试误判 TencentFetcher=MockFetcher 而 fail。改走 `monkeypatch.setattr`(自动还原),全测试 **61 passed**(smoke+golden+adr002-equiv+render-equiv+arch/registry+indicator+旧 report_renderer 包)。golden/adr002 测试本就用 monkeypatch,安全;唯新 render 测试踩坑。
- **commit** `081858d`(7 文件,+1817/−1282),**已 push** 分支。全测试 61 passed。`origin/main` 仍 `be0f177` 未动。
- **有意未做**:部署副本未重打包(分支重构中,待 promote 由 pack_all 从仓库统一再生——report_presentation.py 随 trader_shared 包自动包含)。

## Promote 收尾(2026-07-13 ~11:50,用户"可以"=批准 promote)
- **已 promote**:`git branch -f main refactor/trader-architecture`(main 指针移到 081858d,等价于 fast-forward,不切分支不碰工作树文件,避免被未提交 MEMORY.md 阻塞 checkout),`git push origin main`(be0f177→081858d 干净 ff,无合并无 PR)。
- **重打包部署副本**:`pack_all.py` 从仓库源码生成 4 个 skill zip(trader/t0/review/daily_briefing,digest=c3658c03,DI=ok)并 auto-install 到 `~/.hermes`(各自动备份到 `~/.hermes/skills/.backup/`)。旧 hermes/workbuddy 副本**皆无 `trader_shared/` 目录**(重构前结构),证实重构从未部署到运行中的 agent——promote 正好把它们拉齐到新架构。
- **workbuddy 同步**:旧副本备份到 `~/.workbuddy/skills/.backup/trader-20260713-114449`(3.3M),删除旧后从同一 `trader.zip` 解压(与 hermes 100% 一致)。两副本导入冒烟全 OK:`build_report`(来自 report_builder)/`render_markdown`(来自 report_presentation,经 report_builder re-export)/PluginRegistry 5 插件(chanlun/wyckoff/momentum/supertrend/vwap)齐全。
- **当前状态**:`origin/main`=081858d(已含全部 4 个 ADR 重构);分支与 main 同 commit;`origin/main` 不再孤立。
- **⚠️ 测试隔离深化修复(隐藏坑,重要)**:promote 后批量跑 pytest 暴露 EXIT=1,但每个文件单独跑全过(61 passed)。根因:golden/adr002 测试**裸调用 `set_provider(_MockProvider())`** 改写 `data_provider._provider`(小写!)模块全局 + 写 `os.environ["TRADER_DATA_PROVIDER"]`,二者均非 monkeypatch 可还原(monkeypatch 只能还原函数/属性,还原不了被调用写的值)。golden 跑完泄漏 mock+env 污染后续 adr002 使其 hang/崩溃,而单独跑却过——典型**跨文件全局状态污染**。
  - **修复**:`conftest.py` 加 autouse fixture `_isolate_data_provider`,每测试前后保存/恢复 `_provider`+env。批量同 session 模式现 **61 passed(54.63s)**。注意变量名是小写 `_provider`(首版笔误大写 `_PROVIDER` 导致 AttributeError 全 ERROR)。
  - **教训(写入长期记忆)**:凡测试内 `set_provider()`/`set_fetcher()` 等改写模块全局或环境变量的,**必须配 fixture 恢复**,不能只依赖 monkeypatch。这与 ADR-003b 时的 fetcher 裸赋值教训同源(都是"全局改写需显式还原")。
- **待 commit**:promote 收尾的 conftest 隔离修复尚未提交(本段记录时 conftest 已改未 commit)。

## 当晚盘点核实(用户"还有什么没有做")
- **git 核实结论**:工作树干净(仅 `.reasonix/`/`reasonix.toml` 等 IDE 元数据改动 + 未跟踪 `reports/`/`.mimocode/plans/`/`.zcode/plans/`,均非架构改动);`9e86e25` 已含 conftest 隔离修复并 push 到 `origin/main`;`main` 与 `refactor/trader-architecture` 均 = `9e86e25`,无未提交/未推送代码。日志 89 行"待 commit"已过时——conftest 收尾实际已随 9e86e25 提交。
- **架构重构线代码层面 100% 收尾**:ADR-001/002/003/003b/004 全 Accepted 且落 main。无"该做漏做"。
- **真实未做项(已知债/推迟)**:① `trader_test`(~/.hermes/skills/trader_test)仍带 import Q 旧 bug 未修;② `test_contract.py` 3 项契约漂移失败未修(接口变契约未跟,勿盲目对齐);③ 数据源迁移(通达信 MCP 替腾讯 HTTP)未做;④ CI 门禁未建(文档第5步最后项);⑤ 分支可删可选;⑥ ADR-004 无独立文档(仅 architecture-review 描述)。
- **跨系统待评估(非架构范围)**:Mistery skill 替换 trader、Agent-Reach 集成、OpenCode/OpenChamber 留存评估。

## CI 门禁落地(用户"加ci门禁吧",~12:2x)
- **方案选定**:远程是 Gitee(非 GitHub),故用**版本化 pre-push hook**(而非 GitHub Actions);Gitee Go 需网页配不能纯文件启用。hook 每次 `git push` 前跑离线核心回归集,红则拦截(覆盖"绕过"场景)。
- **交付**:`scripts/run-gate-tests.sh`(锁定 7 个离线/无凭证/确定性测试文件,基线 **68 passed/1 warning/~63s**) + `scripts/git-hooks/pre-push`(版本化 hook) + `docs/ci-gate.md`(范围/取舍/扩展/CI 迁移说明)。
- **启用**:`git config core.hooksPath scripts/git-hooks`(本机已设);commit `43524a3`(未 push origin)。
- **隐患修复**:commit 先落 `refactor/trader-architecture`,`main` 仍 9e86e25 → 若切回 main,hook 找不到 `scripts/run-gate-tests.sh` 会报错阻止所有 push。已 `git branch -f main refactor/trader-architecture` **本地快进 main 到 43524a3**(零风险 ff,不碰远程),main/refactor 均含门禁文件,隐患消除。
- **门禁范围取舍**:不跑全量 80+ 测试(大量依赖网络/凭证 tushare/tdx/腾讯,离线会红/超时,历史有沙箱 SIGKILL 137);门禁守"行为不变"非"行为正确"(等价性闸门固有代价,基线带 bug 会锁成绿);`test_contract.py` 3 项漂移失败未纳入(纳入会永久红)。
- **待用户拍板**:是否 `git push origin main refactor/trader-architecture` 同步门禁到远程(默认不 push)。

## 三方独立评审(用户"派两个agent看业务/代码+一个裁定",~13:1x)
- **范围**:`9eeaf74..HEAD`(昨天 07-12 功能批次 + 今天 07-13 重构+CI 门禁,共 19 commit)。主战场 be0f177..HEAD(重构),次要 9eeaf74..be0f177(只标风险)。
- **业务评审结论 go-with-conditions**:门禁 68/68 绿;三评委组合+HMM 权重/展示型隔离/止损只紧不松/中线升序均 intact。抓出 **P1**:① Tushare 主源 `fetch_quote`(`data_provider.py:542-561`)不返回 `trade_date`,静默绕过 `report_builder.py:141-163` 盘中 bar-append → 生产阶段/ATR 可能误判(属 07-12 Tushare 批次债,非本重构引入);② 离线门禁仍漏真实网络(`_fetch_sector_data`/`cyq_perf` 发真实 HTTP,非确定)。另:不变量#1 实际第三评委是 vpf 非 wyckoff(威科夫已移到中线,`fusion_core.py:514` docstring),项目记忆需更新。
- **代码评审结论 go-with-conditions**:实测 68 passed/5 warnings/20.95s;import 冒烟 ok;py_compile 8 文件 OK;87/88 模块可导入(唯一 signal_rule_engine 缺 yaml,非代码缺陷)。抓出 **P1**:① `__init__.py:16-19`+`:67-70` 仍残留 `sys.path.append`+裸名别名 shim(ADR-001 目标未 100% 兑现,过渡期);② `cache_utils.py:400-410` 残留 sys.path 注入且无受益者(死 hack);③ ADR-002 文档自相矛盾(Status 已落地/正文不实现)。midline 有意隔离、Momentum nudge 去重、builder→presentation 单向分层均确认正确。
- **裁定 SHIP-WITH-CONDITIONS**:重构本身达标,但分支携带 Tushare 切换有生产信号静默退化风险 → **条件未满足前禁推 main**。TOP3 风险 R1(Tushare trade_date 静默绕 bar-append,条件性阻断)/R2(门禁漏网+等价锁行为非正确)/R3(卫生债+双副本漂移+midline 未全局)。Push 建议 NO-WITH-CONDITION:若必推→先修 R1→加固 R2→PR 合并 main→合并前 pack_all 同步两副本核对 digest。
- **报告落盘**:`reports/review-business-2026-07-13.md` / `review-code-2026-07-13.md` / `review-adjudication-2026-07-13.md`。
- **行动项(待用户定优先级)**:修 Tushare trade_date 兜底 → 硬门禁 seam(整体 mock tushare_client)→ 清 ADR-001 残留 path hack + 修 ADR-002 文档 → 同步双副本 → 更新项目记忆(第三评委=vpf)并人工复核 golden 正确性。
- **注**:本重构(be0f177..43524a3)未引入任何 P0;所有 P1 均是过渡期残留(07-12 Tushare / ADR-001 遗留 sys.path)或门禁加固项,非重构回归。

## R1 已修(用户"先修",~13:26)
- **改动**:`TushareProvider.fetch_quote`(`data_provider.py:542-574`)返回字典补 `trade_date` 字段。遍历 Tushare query_realtime 可能的字段名(date/DATE/trade_date/TRADE_DATE)优先取;全无则兜底 `datetime.now()`(盘中实时 quote 就是今天获取的,取今天日期语义正确)。
- **效果**:盘中 bar-append(`report_builder.py:141-163`)不再被静默跳过 → 阶段/ATR/结构判定含今日行情。
- **验证**:门禁 68 passed / 1 warning / 65.29s(与基线一致),零回归。
- **commit** `a71825f`(分支 `refactor/trader-architecture`,未 push main)。
- **裁定条件解除**:R1 修复后,"条件未满足前禁推 main"的阻断条件已消除。后续优先级:R2(门禁硬 seam 加固)→ R3(卫生债+文档)。

## R2 已修(用户"可以",~13:31)
- **改动**:`test_build_report_adr002_equivalence.py` + `test_build_report_golden.py` 的 fixture 均补 `tushare_client.get_client` + `chip_data.get_cyq_perf` 的 monkeypatch mock(`_UnavailableClient` 桩)。与 render_equivalence 测试的处理方式一致。
- **效果**:门禁 68 passed **0.65-0.75s**(从 65-80s 降到),真正离线确定,不再受网络/超时抖动影响。Push hook 首次端到端正常工作。
- **commit** `4046dde`。`origin/main` = `4046dde`(R1+R2+架构重构+CI门禁全部推入主支)。
- **R1+R2 已闭环**,阻断条件解除。剩余 R3(卫生债+文档)为非阻断,待排期。

## R3 已修(用户"修",~13:48)
- **代码**:`cache_utils.py:warm_pool_cache` 删死 sys.path 注入(目录已不存在 + __init__.py 已覆盖 scripts/ 路径)。
- **文档**:ADR-002.md 的"决策:推迟"→"执行决策(已落地)", Consequences/未做后续 同步更新。
- **记忆**:MEMORY.md 融合层三评委 wyckoff→vpf(价量资金),威科夫已迁中线。
- **验证**:门禁 68 passed **0.74s**。
- **commit** `c103063`。`origin/main` = `c103063`(R1+R2+R3+架构重构+CI门禁全部推入主支)。
- **R1/R2/R3 全闭环**,裁定全部解除。
- **双副本同步**:`pack_all` → hermes 自动装 + workbuddy 从同 zip 手动解压,digest=9d00d237,两副本 diff 零差异。
- **分支清理**:`refactor/trader-architecture` 本地+远程已删除,`main` 为唯一主支。
- **总收**:仓库 +10,845/−3,805 行,89 文件,22 commit,全部推入 origin/main。
- **R1/R2/R3 全闭环**,裁定三条阻断/条件全部解除。

## 14:05 — 威科夫 phase 修复：展示层 + 跨日持久化状态机（A+B）

**背景**：`wyckoff_analysis()` 的 `_detect_phase()` 已计算 phase/phase_label，但下游报告层完全不消费 → 用户感知"威科夫没有状态"。

**展示层（A）**：`format_wyckoff_oneline()` 新增 `show_phase=True`，开启后输出加 phase 前缀（如 `积累期 C（测试：Spring）·`）。已更新 3 个调用点（report_presentation.py:480、report_core.py:308/976）。

**跨日持久化状态机（B）**：新增 `~/.trader/wyckoff_phase.json` 按 symbol 存储 phase。`_transition_phase()` 规则：无旧状态取新 → 新 phase=none 保持旧（不降级）→ 同方向只升级 → 反方向仅强信号翻转。在 `wyckoff_analysis()` 内 `_detect_phase()` 之后调用持久化过渡覆盖瞬时推断。

**不改短线融合层**（第三席仍是 VPF，wyckoff 留在中线周线）。

## 14:20 — 短线策略评审修复（3 个策略问题）

**评审产出**：分析了短线三席（chan/momentum/vpf）+ 融合层 + 决策层 + 结构层的完整管线。

**已修**：
- P1(删死代码): `_check_theory_breakout` 删整段威科夫验证块（短线已换 VPF，reason 恒为占位符）。commit 95e468d。
- P2(很差偏斜): HMM regime="很差"时权重全零→score=0→"持股观望"极危险，加 -0.5 默认偏斜→"空仓/止损"。commit 95e468d。
- P3(注释): strong_bearish_chan 仅匹配一类卖/顶背驰，加注释说明有意设计。commit 95e468d。
- ①(阈值调regime): score_to_action 偏弱时正阈值 +0.10，减少弱势假信号。commit a0f7215。
- ②(映射偏差): "持股观望"→"防守观察"代替"等转强"，消除中性隐含看多偏差。commit a0f7215。
- ③(资金时效性): fund_features 增加 latest_fund_date，超 5 日自动降级。commit a0f7215。
- ④(VPF对称化): 多空置信度拉平（连续3日 0.75/0.75）、合成逻辑对称（强信号多空同权，分歧取高置信度方）。commit e047a6a。
- ⑤(中短仲裁): 中线偏多时"很差"的"空仓/止损"降级为"减1/3 (中线偏多)"，不平中线看好的仓位。commit 1befc0c。
- ⑥(中线战略评审): 发现 P1 周线威科夫用日线参数、P2 方向不看置信度、P3 strong_bear优先级过高、P4 quality不展示、P5 缺少chase_ok。
  除P3外全修：P1 wyckoff_analysis 加 timeframe 参数缩放 lookback；P2 chanlun_midline_dir 要求 conf>=2；P4 展示结构质量；
  P5 midline_structure 加 chase_ok/risk_chase/reward_chase。commit 4e61fed。
