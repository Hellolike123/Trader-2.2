# 2026-08-04 工作日志

## 威科夫下一轮 epic（A/B/C 三方向）实现完成

法源：`docs/plans/wyckoff-epic-vol-phase-verif-handoff.md`（HEAD 1e64995 已入库）。

### 🔴 重大实测发现（推翻 FDE 轮假设）
**腾讯日线 volume=手**，非「=股」。证据：
1. amount 交叉验证（amount≈vol×100×close，偏差<3%）：000001/600519/300750/000858/600036/601318/688248 等 mootdx/tushare 缓存日线全为手；
2. 腾讯实时 qfqday（web.ifzq.gtimg.cn，即代码 TENCENT_FQKLINE_URL）：601398 08-03 vol=5,128,943 ≈ 缓存 3,689,522；600519 08-03 vol=36,147 ≈ 缓存 55,127；000001 08-04 vol=1,221,130 ≈ 缓存 2,024,978 —— 与 amount 确认=手的 mootdx 缓存同量级。
结论：全源日线（腾讯/sina/mootdx/pytdx3/tushare）单位一致=手。FDE 轮「腾讯日线=股」假设错误 → 其周线出口 ×100（=股）与日线（=手）存在跨周期绝对值 100× 差异（遗留，A-P4 冻结未动，列入 workflows/phase-scan-audit/README.md §2 裁决项）。

### 方向 A（commit 7039683）
- A-M1 按「以源码/协议证据为准」条款**跳过 fallback ×100**（全源=手，×100 反向制造失真）；vol_unit 打 **"lot"**（非 handoff 字面 "share"）。
- light_data.py：`_DAILY_VOL_UNIT="lot"` + `_stamp_vol_unit` 公共 helper；腾讯成功路径写缓存前打标；三个 fallback 分支不乘仅打标。
- 测试：`tests/test_daily_fallback_volume.py` 6 项。

### 方向 B（commit 17b5984）
- 审计报告：`workflows/phase-scan-audit/README.md`（TP-1~TP-7 逐项）。
- 唯一真实撕裂点 = AR 子窗锚（P-M4 剥离 sc_anchor 后子窗冷启动重算 SC，与主流程统一锚不一致）→ `_ar_verdict()` 统一：统一锚 remap 进 wide_bars 单次评估；出界退化 signals；无锚保留原滑窗（P-M3 兼容）。events 零改动（B-P3）。
- ARE（BC 自包含）/SC legacy/ST（广义 ST 已 I-M1 统一）/事件簇（已 I-M3 统一）/周线半幅（数据频率适配）→ 非撕裂。
- 测试：TestArUnifiedAnchorB 4 项；wyckoff_core._run_with_fakes 补 _detect_ar fake（语义不变）。

### 方向 C（commit 937ad1e）
- `tests/test_wyckoff_realstock_verification.py` 9 项：直读 ~/.trader/cache 四票（688248/600519/300750/601398）日线+周线，wyckoff_analysis 离线跑；缓存缺失 skip；禁 RS（monkeypatch WYCKOFF_RS_ENABLED，防 index_weekly_bars=None 触网）。
- 实测：南网日线 TR 0.417/BC 亮/dist_conf=False/sos_kind 键在；周线[-2] vol==日线同周 vol 和（比值 1.0）；茅台日线+周线 SC 失效文案「已失效」；四票无 acc_conf∧failed 并存。

### 验证
- 门禁 run-gate-tests.sh：**736 passed, 4 skipped**；golden_diff_gate check 全绿（无漂移）；wyckoff 全量 397 passed；新测试 6+4+9=19 项全过。
- 提交：A/B/C 各自独立 commit，**未 push**（约定）。

## 第二轮：对照 wyckoff-sos-修复交接说明.md 核对（16:30）

- **9 bug 修复代码层全部落地核实**（A:1934 单日爆发SOS / B:491 TR fallback / C:fresh_floor+_after_sc / D:FDE轮 / E:本轮A / F:include_failed失效文案 / G:core作废 / H:BC_SCAN_BARS=90 / I:P-M2+P-M4+本轮AR统一）。
- **🔴 运维缺口修复**：此前只回灌了 trader skill 安装位，**wyckoff/t0/review × ~/.workbuddy+~/.hermes 共 6 个引擎副本全部滞后**（缺 P-M2/P-M4/本轮A/B，纯滞后无分叉）→ 已整文件回灌 light_data.py + wyckoff_phase.py，8 安装位全一致。
- **测试缺口补齐**（commit 0f0c757）：① TR fallback 单元测试（下跌→横盘→突破不再 None，upper≈44.5/lower≈41.5）；② 南网标本端到端（合成 08-03 突破日，TR+SOS thrust+dist=False+SC→AR 链）；③ 簇 SC 后旧派发失效（_after_sc 兜底）。wyckoff 409 passed、门禁 737 passed。
- 交接 §11.1「周线 vol≈8449817」期望基于「日线=股」错误前提 → 维持本轮实测结论（全源=手），不采纳。

## 第三轮：3 项 Agent 建议落地（双 Agent 闭环，16:40）

- 计划：`docs/plans/2026-08-04-agent-suggestions-handoff.md`（写/查双 Agent handoff）。
- 写 Agent（commit c8ee751）：① `scripts/sync_skill_engines.sh`（仓库 trader_shared → 8 安装位，只同步已存在文件、diff 校验、--check 只报不改、幂等、macOS bash3.2 兼容）；② wyckoff_render.py `_warn_contradictory_phase_a` 渲染前防御告警（accum∧phase_a failed→warning 不改输出），挂 card/slim/detail 三档；③ automation-1784136710106 prompt 价位刷新（46.88/50.66 → 45.50 突破位/44.5 TR 上沿/41.5 TR 下沿/50.66 MA250，路径 A/B/C 更新）。
- 查 Agent：全 ✅（sync 脚本规则/幂等/--check、render 不改输出、prompt 价位与 08-03 结构一致、范围受控、未 push）；2 条非阻塞。
- 父 Agent（commit 2984f56）：修查 Agent 建议 1（基准位缺失但其它位存在→告警，模拟验证通过）；建议 2（rank 挂 check）不采纳——rank 行无矛盾字段，死代码。
- 验证：wyckoff 412 passed、门禁 737、golden 无漂移；未 push。
- ⚠️ automation-1784136710106 不依赖 wyckoff 引擎灯（tdx MCP + LLM 三大定律推理），降级预案前提不成立。

## 第四轮：#5 周线 ×100 回退裁决（双 Agent，17:00）

- 裁决：回退 FDE 轮周线 sina/mootdx 出口 ×100，周线全路径统一「手」，vol_unit="lot"（全源日线+周线=手）。
- 证据：全源日线=手（实测）+ 周线消费方全比值（tr_baseline_volume 仅用于量比/相对阈值，回退零检测器影响）+ 当前周线内部不一致（sina 出口=股 vs 聚合=手）。
- 写 Agent（commit 0c20d8c）：fetch_weekly E-M1 去 ×100 打 lot、E-M2 聚合打 lot、E-M3 改 `!= lot` 强制回源（旧 share/无标记都迁）、删 _stamp_vol_unit_share；test_light_data_weekly E/D 组断言改不乘+lot；FDE handoff E 节加裁决标注、审计报告 §2 改已裁决、MEMORY 更新。
- 查 Agent：全 ✅ 无必改（diff 42 行全在 fetch_weekly，wyckoff_events 未动）。
- 父 Agent（commit 0579fb5）：裁决 handoff 入库。验证：light_data_weekly 7 + wyckoff 412 + 门禁 737 + golden 无漂移。未 push（待征询）。

## 第五轮：交接文档更新版（11 bug）→ Bug J/K 修复（17:35）

- 文档更新：9 bug → 11 bug，新增 Bug J（trader_shared 传染 6 skill）+ Bug K（daily_briefing 缺 run_analysis 崩溃），§14 验收指出「已安装 skill 未重打包」。
- **发现此前回灌遗漏**：我只回灌了 4 skill × 2 文件（light_data/wyckoff_phase），9 bug 主体 wyckoff_events.py 及 chanlun/daily_briefing 完全滞后 → 已通过扩展 sync 脚本（SKILLS 4→6）全量同步 12 安装位 × 全部引擎 .py（commit 3390050）。
- Bug K 修复：复制 trader 包 {run_analysis, final_pool, config} + pool_cmds/ 进 daily_briefing（仓库+2 安装位）；**修正 briefing.py:92 既有 import bug**（from final_pool import score_report → from pool_cmds.scoring import score_report，原 import 任何上下文都失败）；smoke 全过。
- 验证：门禁 737、golden PASSED、sync --check 12 位全一致。commit 3390050 未 push。

## 第六轮：Bug L（daily_briefing 决策标签拼 dict）修复（17:42）

- 根因：briefing.py:762 `f"{major_stage}+{momentum}"`，momentum 是 dict（direction/score）→ 整坨 dict 拼进 stage_label。
- 修复：新增 _momentum_label()（dict→「偏多(70)」摘要；str 原样；空→""），:762 主行 + else 分支改用；同步 2 安装位；9 组单测全过；门禁 737。commit 009fe75 未 push。
- 待 push 累计 3 个：3390050（Bug J/K）、0ed68c8（日志）、009fe75（Bug L）。

## 第七轮：skill ZIP 重打包（17:52）

- 打包 6 个 skill（chanlun/wyckoff/daily_briefing/review/t0/trader）→ `03-安装包-dist/releases/<ts>/<skill>.zip`，从**安装位**打包（12 位引擎已全同步，zip 即最新版；排除 __pycache__/pyc/DS_Store）。
- 验证：wyckoff/daily_briefing zip 内 wyckoff_events.py + light_data.py 与仓库 diff 一致；daily_briefing.zip 含 run_analysis.py + pool_cmds/scoring.py（Bug K）；SKILL.md 在。
- ⚠️ 记录：`scripts/reset_install_skills.sh` 已过时——package_dir_for 映射旧目录名（01-单票分析-trader 等，已不存在），全仓库无 package_skill.py（打包脚本缺失/删）；将来若恢复「打包回归一体化」需重建打包脚本（Bug J 修复方向③）。
- **修正（17:56）**：按用户指出重打**两套**（hermes / workbuddy，唯一差异=config.json `trader_host`），releases/0804-1756/{hermes,workbuddy}/×6 zip；验证两套 config 正确、引擎=仓库、daily_briefing 含 run_analysis。zip 被 gitignore 不入库（仅 releases/ 目录结构在产物侧）。

## 数据层选型评估：iFinD / 讯投(XtQuant) 能否替换抓数据环节（22:50）

- 用户问：另一 AI 建议用 iFinD 替换"所有抓数据+数据消费"；并给讯投链接(dict.thinktrader.net/freshman/rookie.html)。
- 澄清：**讯投=迅投 QMT/miniQMT 交易终端(xtdata行情+xttrade交易)，需本地Windows客户端，Mac跑不了；属交易执行层，非数据层**。iFinD=同花顺机构数据终端，HTTP-API 跨平台(Mac可用)、只读不写。两者解决不同问题。
- 结论：iFinD 只能替换"抓数据"，**不能**替换数据消费(缠论/威科夫/筹码/fusion 吃归一化bars，与分析源无关)。
- 改动量：中等。DataProvider Protocol + UnifiedProvider(backend) + set_provider 已是DI架构 → 分析层零改动；只需新增 IfindProvider(约13方法+token刷新+QPS限流+429/401重试+代码转.SH/.SZ)，约400-600行；get_provider工厂加分支几行。回测引擎硬编码TencentFetcher是痛点(会被QPS20限速+配额耗尽)。
- 风险：①iFinD官方不面向个人直售(商务签约/万元年起，淘宝租token违规)；②QPS整体20/单函数10，重回测会限流+耗尽月度单元格配额；③token 7天过期需自动刷新；④代码须600030.SH格式；⑤不能二次分发。
- 建议：维持现状(腾讯/新浪/mootdx免费兜底，Mac已能跑)；若确有机构账号，把iFinD加成可选backend(env切TRADER_DATA_PROVIDER=ifind，默认腾讯)，回测暂勿接。未动手改码。

## 澄清：用户贴的"另一个AI文档"实为《AI Agent量化交易数据接入建设建议书》（23:00）
- 非 iFinD 替换说明。描述的是 `MiniQMT(xtquant)→MCP风控中间层→AI Agent+Backtrader T+0` 自主交易系统蓝图；两融信用账户、实盘下单经MCP风控。
- 与 Trader3.0 现状核对（已读 light_data.py 验证）：
  - ✅已有：K线含amount(401/777/815，VWAP可算)、盘口L5买1-5/卖1-5+失衡(828-848)、1分钟线支持(_resolve_sina_kline_scale "1"→"1")、wyckoff模块(SC/ST/Spring)、BOLL/ATR/EMA/VWAP指标。
  - ❌缺失：持仓/两融/委托数据(无券商连接)、逐笔Tick(可选)、MCP风控层、实盘下单。
- 文档硬伤：①xtquant需Windows QMT客户端，用户Mac跑不了(硬 blocker)；②Backtrader与本项目已否决backtrader、自研intraday引擎的路线冲突；③两融+实盘对纯分析系统是全新量级；④文档把"讯投(交易)"与"数据"混淆，威科夫当待建(实际已有)。
- 结论：把文档当"数据需求清单+未来目标架构"用；立刻可吸收其"数据供给硬性规则"(标准错误码/订阅白名单/amount必填)；勿现在搭xtquant+MCP+Backtrader+两融全栈。未改码。

## tushare token 连通性实测（22:58）
- 用户提供新 tushare token。在 **WorkBuddy 沙箱**内实测：HTTP/HTTPS 均 HTTP 200 + code:0，token 有效（trade_cal 返回真实数据）。
- ⚠️ 关键：沙箱 TCP 探测 IP=198.18.0.161（RFC5737 测试网段）→ 走沙箱代理，**不代表 Mac 本机连通性**。Mac 本机历史仍实测 `api.tushare.pro` Connection refused。沙箱通 ≠ Mac 通。
- 系统现状：`tushare_config.local.py`（已被 .gitignore 忽略）**已存在一个 tushare token**（加载顺序：env TUSHARE_TOKEN > local.py > config.json > 空）。此前退化腾讯是因 Mac 网络不通，非缺 token。
- 安全：用户 token 已在对话明文出现，属敏感凭证，不写 memory/代码库；配置应走 env 或 local.py，勿硬编码进被 git 跟踪的文件。
- 待用户在其 Mac 终端原生验证连通性后再决定是否启用新 token。未改码。

## tushare_client 沙箱失败根因（23:02 追加）
- 裸 urllib 在沙箱调 tushare `daily` 成功返回茅台 08-03/04 真实日线（HTTP200/code:0，含vol/amount）→ **沙箱有外网、tushare 可达**。
- Trader3.0 `tushare_client` **有意为之**绕过系统代理：`bypass_http_proxy_for_market()`（:58）清除 http_proxy/https_proxy 并设 NO_PROXY，因 Agent/IDE 沙箱常注入 http_proxy 会把腾讯实时源 stockai888 拦 403；`_query_http`（:296）再硬编码 `proxies={'http':None,'https':None}` 双重保险直连。目的=行情通道一律直连、不被代理隧道误伤。
- 但在**当前 WorkBuddy 沙箱**，tushare 出口恰需走注入的 http_proxy 才能出外网 → 这套'为沙箱好'的设计反而挡住 tushare（client 调 daily 返回0行）。urllib 默认读 http_proxy 故裸连通。
- **已改**（23:13）：新增 `_resolve_proxies()`（:84附近），`_query_http` :296 改 `proxies=_resolve_proxies()`；`TRADER_KEEP_HTTP_PROXY=1` 时返回 None 跟随环境代理，否则强制直连（保留作者防御）。未 push。
- **真正根因**：项目默认 `TUSHARE_API_URL=https://fastapic.stockai888.top`（tushare_client.py:30 + tushare_config*.py），是第三方镜像非官方；用户的**官方 tushare token 在该镜像返回 401 不认**。沙箱实测官方 `api.tushare.pro` + 用户 token 可通（裸 urllib/requests 均 200/code:0）。
- **完整解锁**（沙箱实测通过）：`TUSHARE_API_URL=https://api.tushare.pro` + `TRADER_KEEP_HTTP_PROXY=1` + `TUSHARE_TOKEN=用户token` → tushare_client 拉到茅台日线（2 行）。默认 URL 保持 stockai888（不破坏设计），URL 用 env 覆盖。待用户决定是否固化 local.py + 同步安装位。
