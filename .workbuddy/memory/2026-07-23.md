# 2026-07-23 工作日志

## 回测选型 + build_report 前视偏差验证

- 用户咨询策略回测用 backtrader 还是自研。结论：自研轻量框架为主，不引 backtrader（数据范式冲突、维护差）。已写入 MEMORY.md 回测段。
- 验证 `build_report` 能否按历史某天截断（前视偏差命门）：**不可行**。
  - `build_report` 内部硬调 `load_market_snapshot(days=LOOKBACK_DAYS)` 且不接受数据注入。
  - 数据层 `load_market_snapshot`/`fetch_qfq_daily` 只有 `days` 无 `end_date`；tushare 路径 `end_date` 硬编码 `datetime.now()`（data_provider.py:603）；腾讯/新浪是"最近 N 天"模式。
  - 另有 fund_flow（全量最新）、market_env/regime（截至今天）两处未来数据入口。
- 回测正确接入：绕开 build_report，直接调 `plugin_registry.analyze_all(current_t, bars[:t], ...)`（吃 bars 参数，传切片即无前视）+ `fusion_core.merge_decisions(...)`；需冻结 market_env 到 t 日、current 用 bars[t]['close']。现有 backtest_chanlun.py / backtest_t0.py 已走此路。
- 仅极端的几百参数×几百票向量化扫描才考虑 vectorbt；tick 级真撮合才考虑 nautilus_trader。

## 实现最小 BacktestEngine 骨架

- 新建 `02-共享模块-shared/scripts/backtest_engine.py`：复用生产 `plugin_registry.analyze_all` + `fusion_core.merge_decisions`（fusion_from_cards="classic"），`bars[:t+1]` 切片 + 冻结 `market_env`（`assess_as_of` 切片中证1000历史复算 level/HMM）+ 次日开盘撮合 + T+1 + 固定 8% 止损 + 满仓/空仓两态权益会计。
- 踩坑（已写入 MEMORY.md 回测段）：① Mac 强制 `set_provider(UnifiedProvider(backend="tencent"))`；② 必须绕过 `get_day_scoped_bars` 的「当日快照缓存」（曾吐陈旧 2 根），直接 `TencentFetcher().fetch_qfq_daily(code, days)` 拉全量；③ `TRADER_CHAN_NESTING=0` + `quote["_bars_5m"]=[]` 防 analyze_all 偷拉未来 30m/5m。
- 验证：茅台 300 天 +3.96%/盈亏比 2.88/4 笔；平安银行 -17.59%（弱势符合预期）/7 笔。无前视偏差、会计正确、可泛化。
- 已知边界（骨架未做，已在文件头 KNOWN_LIMITATIONS 列清单）：涨跌停、ATR trailing 止损、cards 模式对齐实盘、仓位缩放、指数历史依赖腾讯/新浪。

## BacktestEngine v2 升级（三件全做 + 数据健全性快检）

- 重写为 `02-共享模块-shared/scripts/backtest_engine.py` v2，落地三件：① **cards 对齐实盘**（默认 `fusion_from_cards="cards"`，`signal_at` 内 `build_chan_card`/`build_momentum_card`/`build_vpf_card` 三卡喂 `merge_decisions`，与 `build_report` 同源；`--no-cards` 退回 classic 提速）；② **ATR trailing 止损**（Wilder ATR×倍数，只上移、地板=初始硬止损，替代固定 8%）+ **涨停买不进/跌停卖不出**（按代码自动 10%/20%）；③ **参数扫描**（`--scan`：`atr_mult×stop_pct×step` 18 组网格，信号只算一遍，`ProcessPoolExecutor` 多进程并行跑撮合层复用同一份 `signals`/`bars`）。
- 关键架构：信号生成与撮合解耦——信号最慢只算一次，扫描只重跑纯本地撮合。
- 加**非破坏性数据健全性快检**：收盘价比中位数 >5× 告警（抓 `TencentFetcher` 偶发 100× 缩放坏点）。A股单日 ≤20%，>5× 几乎必为数据错误。
- 验证：茅台 300 天 cards +3.98%/夏普 0.56/盈亏比 2.12（同进程两次调用逐位一致，确定性 OK）；平安银行 -2.07%（弱势亏损合理）。扫描结论：ATR×2.5+step1 最优，step=2 因漏信号明显转差。
- **🔴 数据层非确定坑**：`TencentFetcher().fetch_qfq_daily` 跨进程偶发返回不一致数据（某根 bar open/close 被缩放 ~100×）。进程内 `run_backtest` 完全确定（两次调用一致），故扫描（主进程抓数一次传 worker）不受影响；但不同独立进程各自抓数可能拿到不同数据集，单次报告若撞上坏点会静默污染。引擎已加 >5× 告警兜底；根治需修 `TencentFetcher` 缓存/去重或回测前落盘缓存复用。

## BacktestEngine v2.1 — 实盘风控接入（point-in-time，无前视）

- 用户要求：把 fund_flow/板块/extend_* 历史数据接入，让回测风控与实盘一致（之前全传 None）。
- 查码定位：`analyze_all` **不接收** fund_flow/extend_* 参数；veto 发生在 `fusion_core.merge_decisions(fund_flow_data=..., extend_sentiment=..., extend_fundamental=...)`。引擎原 `fund_flow_data=None`。
- **已接入三路（全部按 t 切片，无前视）**：
  1. `fund_flow_data`：`_fetch_risk_history` 调 `fetch_fund_flow(code, days=400)`（有缓存）→ 每 t 取 `slice≤date_t` 喂 `calc_fund_flow_features(sl, cur)` → 连续主力净流出 veto 触发。
  2. `extend_sentiment={"unlocks":[t≤u.date≤t+90]}`：用 `eastmoney_datacenter(RPT_LIFT_STAGE)` 原始拉**全量**解禁事件（不过滤 today），循环内切 90 天窗口 → 解禁风险空仓 veto（与实盘「未来 90 天」一致）。
  3. `extend_fundamental={"shareholder":...}`：仅当 `date_t≥latest_notice_date` 才生效 → 股东户数筹码集中置信加成（point-in-time）。
- `--no-riskdata` 关闭做 A/B 对照。报告打印「风控触发」次数（资金流出/解禁）对账。
- **验证（决定性）**：平安银行风控开 vs 关 → 总 -5.80% vs -2.07%、交易 12 vs 13 笔（veto 挡掉 1 次建仓信号）→ **证明风控真正改变撮合、与实盘一致**。茅台 27 根资金流/7 次 veto，但 veto 日恰为非建仓日故数字不变（合法）。
- 资金流 API 对茅台仅返最近 ~27 交易日、对平安银行返 267 根（2025-06~2026-07）→ veto 生效范围随数据源而定，但**切片 `<=date_t` 保证全程无前视**。
- **out-of-scope（已在 KNOWN_LIMITATIONS + MEMORY.md 标注）**：`extend_sector`/`extend_concept`/`extend_margin`/`extend_northbound` 在融合层仅软修正、非硬 veto，需各自历史源，暂未接入。
- 写代码时踩的假象：针对性核对测试把 `registry` 误传 `None` 致 `analyze_all` 抛异常走 except 返回"观望"，误以为 veto 不触发；用真 `get_registry()` 复测即正常。

## BacktestEngine v2.2 — 落盘缓存 + 北向软修正（用户"都做了"）

- 用户回复"都做了"= 同时做两件：① 接入剩下三类软修正（sector/concept/margin/northbound）；② 加回测前落盘缓存根治坏点。
- **① 落盘缓存（根治跨进程坏点）**：`load_data` 加 `use_cache/force_refresh`，写 `~/.trader/backtest_cache/{code}_{days}_{kind}.json`（日/周/指数三份）。`_sanity_check` 在**写前+读后**都跑「收盘>5×中位数」断言。二次运行显示「日线命中 指数命中」，所有 run 读同一份确定数据，彻底消除 `TencentFetcher` 跨进程偶发 100× 缩放。CLI `--no-cache`/`--refresh-cache`。
- **② 北向 point-in-time 接入**：`_fetch_extend_history` 用 `ak.stock_hsgt_hist_em(symbol="北向资金")` 拉全量（2714 行）→ `日期`→date、`当日成交净买额`(亿)×1e4→万元 `net_wan`，落盘缓存 key `NB/0/nbh`。`_northbound_inputs(date_t)` bisect 切片 `<=t` 求和窗口 5d，喂 `merge_decisions(extend_northbound=...)`。`--no-extend` 关。
- **🔴 关键数据源发现（诚实披露，非 bug）**：`ak.stock_hsgt_hist_em` 的 `当日成交净买额` 列对 **2020+ 全行 NaN**（仅 2014 早期零星有值）。回测窗口 2025-09+ 内北向恒 0 → 落入 `merge_decisions` dead-zone → **real-run 恒 no-op**。合成北向注入验证接线 100% 正确（33 天 confidence 改变，0.094→0.099 = +5%），错在数据源非接线。**不伪造数据**。
- **NaN 防御**：`net_yi = float(_raw) if _raw is not None else 0.0; if net_yi != net_yi: net_yi = 0.0`（nan 在 Python `nan or 0 == nan`，必须显式 coerce，否则下游静默跳过）。
- **sector/concept/margin 仍 out-of-scope**：Mac akshare 实时 `get_sector_data`/`get_concept_data` 对个股→板块分类返回「无数据」；margin 无分票历史。需可联网沙箱或换 tushare 源。
- 修 `_print_report`：`%`-format 规避本 Python 版本 f-string 字面 `(` 解析歧义（曾反复 SyntaxError）。
- **最终回归（无退化）**：茅台 600519（300d, cards）+3.98%/夏普 0.56/5 笔；平安银行 000001 -5.80%/夏普 -0.88/12 笔/44 次 fund_flow veto。与 v2.1 数值一致。

## 审计：南网科技 t0 VWAP bug 修复单（用户"看看是不是有问题"）

- 用户指了一份修复单 `南网科技_t0_skill_VWAP_bug_修复单_2026-07-23.md`（在 `~/Documents/Workbuddy/Trader/`）。
- **结论：诊断主体准确、修复方案对，但「修复指引 note 6」有严重错误（改错位置）。**
- 已逐项核实（读 active 的 workbuddy 副本代码）：
  - ✅ 根因真：`display_indicators.py:27 calc_vwap` 与 `indicators.py:89 calculate_vwap_from_bars` 函数体**确实无任何按日过滤**，直接对全量 `bars_5m` 求 Σ(typical×vol)/Σvol；`indicator_math.py:244 calc_vwap` 只是转发。
  - ✅ `t0_run.py:74` 调 `provider.fetch_5m(sec, datalen=800)`（≈16 交易日 5m）；调用链 `:287 calc_vwap` → `:174 calculate_vwap_from_bars` → `:219 add_level("VWAP",vwap,0.6)` → `:253/:307 买区` → `:677/:776 被阻断/T0净空间不足` 全部行号精确命中（仅 `t0_run.py:148` 实际是 `:149` 偏一行）。
  - ✅ hermes 副本今天 12:45 的刷新只是重新打包，**没修 VWAP**——两边 `calc_vwap` 完全一致（均无过滤）。所以报告没过时，active 副本确实还有 bug。
  - ✅ 5m bar 实际字段是 `date`(YYYY-MM-DD)+`time`(HH:MM)（light_data.py:338/1299），故报告建议的 `_session_bars` 辅助函数能跑通（之前担心的字段名问题解除）。
  - 🔴 **note 6 修复指引错误**：报告说"不要直接改 ~/.workbuddy/skills/t0 运行时副本（重装会被覆盖），请提交正式仓库落地"。但 **t0 skill 运行时用的是自带 bundled `scripts/trader_shared/display_indicators.py`，不是仓库 `02-共享模块-shared/trader_shared/` 那份**（final_t0.py 从 scripts/ 起，sys.path[0]=scripts/，import trader_shared → 解析到 scripts/trader_shared/）。按 note 6 只改仓库，active skill 照样坏。正确做法（契合项目"skill 双安装位修复两边都打"惯例）：直接改 **两份 skill 副本**的 `scripts/trader_shared/display_indicators.py` + `indicators.py`（workbuddy + hermes），可选再同步仓库作 source-of-truth。
  - 🟡 小瑕疵：§A 只贴了 `calc_vwap` 的代码片段，说"两个函数都加"但没贴 `calculate_vwap_from_bars` 的代码（函数级修复会一并覆盖，无碍）；`calculate_vwap_from_bars` 在 `price_point_engine.py:334-335` 还有一处调用（报告只点名 :174），函数级修复自动覆盖；"≈13 交易日"偏保守（4000min/240≈16.7 天）。
- 已向用户指出 note 6 错误 + 给出正确修复路径，并问是否由我落地（两边都改 + 加 `test_vwap_session.py` 回归）。

## 落地修复：t0 VWAP 跨日污染（用户"你来改"）

- **实际改了 5 个文件（去重）：**
  - workbuddy 副本：`scripts/trader_shared/display_indicators.py`（calc_vwap 加 `_session_5m_bars` 辅助 + 调用处 session 过滤 + 越界 warning）、`scripts/indicators.py`（calculate_vwap_from_bars 同样加 session 过滤）、`scripts/price_point_engine.py`（§B 区倒置守卫：买区 main_support ≥ 卖区 main_resistance → 报"数据异常"而非"净空间不足"，买/卖触发器各一处）。
  - hermes 副本（pack_all 目标）：同样三文件逐一 edit（**未 cp 覆盖**——hermes `price_point_engine.py` 比 workbuddy 大 2878 字节/多一处 net_space 调用，已确认逐处 old_string 仍匹配后精准 edit，保留 hermes 其它更新）。
  - 仓库 `02-共享模块-shared/trader_shared/display_indicators.py`：同源 calc_vwap 修复（仓库无 calculate_vwap_from_bars，只修 display_indicators；兼作 source-of-truth + 修 trader 单票同源 VWAP）。
- **辅助函数 `_session_5m_bars`**：取 bar 的 `date`/`datetime`/`time` 前 10 字符作交易日，`max` 取最新日过滤；无日期信息原样返回（保守）。三文件各自就地定义，避免跨模块 import 耦合。
- **回归测试**：新增 `tests/test_vwap_session.py`（旧高价 07-02 66 + 当日 42.x 混合，断言 VWAP 落在当日 42.4~43.2；空输入安全返回 None）。workbuddy + hermes 两份各跑一遍 **ALL PASS**，VWAP 由会被污染的 ~50.92 → 正确 42.7。
- **验证**：7 个改过文件 `py_compile` 全 OK；hermes / repo 功能导入 + 当日过滤断言全过（42.700）。
- **未动 §C 缠论窗口化**：属启发式且会改缠论行为，报告也自评"次要"，本次聚焦决定性修复（§A + §F + §B 防御）。
- **未提交 git**：按惯例默认不 push；如要归档可后续 commit。

## 回测引擎 v2.2 提交 + 南网科技实跑（14:4x）
- **提交两个 commit**：`d75cbae` fix(t0) VWAP 跨日污染+区倒置（仓库 display_indicators + 记忆）；`d83e16c` feat(backtest) 无前视回测引擎 v2.2 基线（backtest_engine.py 1145 行全新文件，独立 commit 不混 VWAP）。均默认不 push。
- **南网科技 688248 实跑回测**（科创板自动识别 ±20%，非 ±10%）：总 +84.10% / 年化 +80.92% / 最大回撤 -23.99% / 夏普 1.95 / 14 笔 / 胜率 71.4% / 盈亏比 4.55 / 均盈+10.05%均亏-5.52%。风控触发 资金流出 veto 5 次；北向显示"历史 2714 天"但窗口内仍 NaN→0 死区（no-op，仅行数）。
- **公平性校验（关键）**：同窗口 buy&hold = +27.84%（251 交易日，首 2025-07-11@32.89 → 末 2026-07-22@42.05；区间 high 74.82 / low 32.43 / 振幅 131%）。策略超额 alpha ≈ +56pp——来自捕获 32→70 上攻段并在 70→32 崩跌前离场。架构三道无前视（bars[:t+1]/regime 冻结/风控 point-in-time）+ 缓存健全性快检均在，单笔均盈仅 +10% 非单笔暴利，判断为"趋势择时合理 alpha"非前视虚高；但**单票单趋势不可外推**（茅台 +3.98% / 平安 -5.80% 对照）。
- **对用户澄清点**：用户说"用我 t0 策略"，但本引擎回测的是**日线信号脑**（analyze_all+merge_decisions，T+1 次日开盘），**非 t0 盘中 5m/VWAP/三重共振策略**（t0 skill 无回测能力）。需在回复里讲清这层区别；若要真·T0 盘中发现有回测，需另建日内引擎。

## 日内 T0 无前视回测引擎（用户"两个都要用"）

- **新文件** `02-共享模块-shared/scripts/intraday_backtest_engine.py`（~640 行）：复用 t0 skill 真实信号脑 `price_point_engine.build_price_point_model` + `realtime_chan.get_realtime_chan_5m` + `chan_core.chanlun_analysis`，做 T+0 日内回放。
- **内存隔离（解决 OOM exit 137）**：抽出 `run_one_day(code,day,...)`，`run_backtest` 协调器每天起一个独立子进程跑（跑完退出回收 t0 大脑内存），父进程只聚合权益。单日子进程峰值 ~85MB；`--refresh-cache` 只父进程抓一次，子进程统一走缓存。
- **🔴 两处保真性 bug（不修则全 0 成交）**：
  1. **Sina 5m 历史缺 13:00 棒**（午休后首根被标成 13:05，50 天全缺）。后果：下午任意棒 `completed_5m_bars` 后 last=11:30 → `data_status` 距 now≥95min → `degraded` → 观察价失效 → 全下午无交易。修：`_fix_lunch_gap` 侦测 11:30→13:05 跳变补 13:00 棒（OHLC 取相邻 AM 收/PM 开，volume 平分），缓存读时也能幂等补。
  2. **未注入日线结构支撑/阻力 `structure_result`**。`build_price_point_model` 的 `find_key_levels` 优先吃 `structure_result`（trader 日线支撑/阻力，weight 1.1，见 `t0_run.py:128` "T0-1 fix"），否则退化到仅日内近价位（今日低点/高点）→ `t0_net_space_pct<0` → 买/卖区恒"被阻断"→0 触发。修：`signal_fn` 用 `daily_upto`（D 之前日线=无前视）调 `build_structure_context` 算 `structure_result` 注入。加 `--no-structure` 可退化对照。
- **诊断结论（关键，先讲给用户）**：t0 是"三重硬共振"（缠论15m/5m 买点 + 威科夫 Spring/无供给 + 动量 RSI背离 **三者同时亮灯**）才 `buy_green`/`sell_red`。南网科技 688248 窗口 2026-05-14→07-23：买侧 `chan_buy` 常真（realtime_chan 买点）、`wyckoff_buy` ~9/49、`momentum_buy` ~3/49，但**三者同时 = 0**；卖侧 `chan_sell=0`/`momentum_sell=0` 结构不可能。→ 回测结果大概率为 **~0 笔成交**（策略本身极保守，非前视 bug）。全量 50 天回测后台跑（task UGVB4g）确认中。
- **⚠️ 版本对齐未决**：引擎注入的是 **workbuddy** 副本 `price_point_engine.py`（已修 strokes UnboundLocalError）。但 hermes 副本有 2026-07-20 的 **Al Brooks 价格行为-first 重写版 `check_resonance`**（三重共振第一席位从缠论换 Al Brooks）。两份已分化。需问用户：实盘 t0 到底跑哪份？若是 hermes 版，回测需对齐（否则共振门控不一致）。

## t0 版本统一为 Al Brooks（用户拍板"都变成al brooks"）

- **同步方向 hermes → workbuddy**：hermes 副本（Jul 20-23）是最新——Al Brooks 版 `check_resonance`（三重共振第一席位=analyze_ab，缠论分支整体删除，strokes bug 天然不存在）+ `today_bars` VWAP 修复 + 各席位 exit_price。同步了 8 个文件：price_point_engine.py / t0_run.py / t0_core.py / ab_price_action.py(workbuddy 原本缺失!) / decision_core.py / schema/v1.py / stage_stops.py / wyckoff_events.py。备份在 /tmp/t0_workbuddy_backup_*。
- **回灌仓库**：`01-功能包-packages/t0/scripts/price_point_engine.py` 落后 hermes 一个纯新增「数据异常」守卫（买区主支撑≥卖区主压力→跨日 VWAP 污染报数据异常），整文件复制回灌。其余 t0 相关文件仓库与 hermes 已一致。未 commit。
- **回测引擎适配**：Al Brooks 版共振不吃 chan_5m/chan_15m（模型内部自算 analyze_ab）→ 裁掉引擎里 `get_realtime_chan_5m`/`chanlun_analysis` 全部计算。**单日 9.6s → 1.3s（7x），全量 50 天仅 52s**。
- **🔴 最终回测结果（Al Brooks 版，688248 南网科技 2026-05-14→07-23，50 天 2450 根）**：**0 笔成交**。席位亮灯抽样：ab_buy 5-9/天、wyckoff_buy 12-26/天、momentum_buy 0-3/天——**卡点是动量席（RSI 12 棒背离）**，几乎不亮且从不与另两席同帧。sell 侧同样 momentum≈0。结论：三重硬共振对单票日内是"极稀疏事件"，或需多票扫描、或动量席条件该讨论放宽（产品问题，非 bug）。
