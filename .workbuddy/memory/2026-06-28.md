
## 第 5 轮 Agent 审查（agent 审 agent）

4 个 agent 并行审查 trader_shared 计算模块，共发现 12 个 bug（P0×2 / P1×4 / P2×6），报告写入 docs/agent-review-round5.md。

**2 个 P0（已验证属实）**：
1. fund_flow_data.py:163-175 — 东方财富 API 字段映射全部错位（agent 用茅台实时数据验证 parts[1]=主力、parts[5]=超大单，代码全错位）
2. chan_core.py:536-539 — 二类卖点误用底部背离标志（macd_divergence_ok 只检测底部背离，却传给 detect_sell_points）

**4 个 P1**：
- stage_positioning.py:149-166 跳空日检测死代码（base_idx 永远返回 0）
- structure_core.py:128-131 ATR 首根K线漏算跳空缺口
- fusion_core.py:478-515 regime="很差" 时 pattern 权重未归零（get 默认值 0.10 偷偷加回）
- hmm_regime.py:59-60 HMM 先验 mu/sigma 状态1/2 标签错位（短输入 3~29 个时大势判错）

前 4 轮已修约 27 个 bug，本轮找到的是更隐蔽的逻辑错误，未发现崩溃性 bug。未发现 bug 的模块：chan_core 笔/中枢、bayesian_fusion、fusion_regime、main_force、chip_distribution、pattern_core、volume_price、volume_profile、expma_status、multi_timeframe_resonance、indicator_math。

## 第 5 轮 agent 审查修复（12 个 bug 全部修复）

**commit**: 4b155c5，8 个文件，+116 / -37 行，722 个测试全部通过

**2 个 P0（已修）**：
- fund_flow_data.py: API 字段映射修正，追加约束关系注释
- chan_core.py: 新增 `_check_macd_for_2nd_sell()` 顶部背离检测，二类卖用独立变量

**4 个 P1（已修）**：
- stage_positioning.py: base_idx 改为 max(0, min(gap_indices)-1)
- structure_core.py: ATR prev_close 提前从 bars[-period-1] 初始化
- fusion_core.py: regime="很差" 显式设 pattern=0.0 + get 默认改为 0.0
- hmm_regime.py: 先验 mu/sigma 正序排列 [牛/震/熊]

**6 个 P2（已修）**：
- structure_core.py: RSI/MACD 背离传入对齐的 _bars
- fusion_core.py: mom_score|None 安全获取
- decision_core.py: 威科夫验证改用 reason 关键词匹配
- momentum_core.py: calc_adx 加 if i > period: 守卫
- hmm_regime.py: len<3 分支 state_id=1 + cache key 含 len(returns)
