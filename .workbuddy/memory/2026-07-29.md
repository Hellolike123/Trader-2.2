# 2026-07-29 工作日志

## Bug 修复（系统排查 + 修复）

### P0 修复
1. **HMM 测试永久失败** — mock bars 缺 volume + 文件缓存短路 mock。双修：
   - mock bars 加 `volume=1000000`
   - `from market_env` → `from trader_shared.market_env`（不依赖 `__init__.py` 裸名别名）
   - 额外 mock `cache_utils.get_cached=None` 绕过文件缓存
2. **7 个模块用错 logger** — 统一从 `logging.getLogger(__name__)` 改为 `from trader_shared._logging import get_logger` + `get_logger(__name__)`：
   - `chip_core.py`, `data_access.py`, `display_indicators.py`, `fusion_regime.py`
   - `report_pipeline.py`, `self_calibration.py`, `t0_candidate_core.py`
   - 另修 `self_calibration.py:390` 残留 inline `logging.getLogger`

### P1 修复
3. **`signal_tracker.py:613` 裸 `except:`** → `except Exception:`
4. **`market_env.py:195` 墙钟 today_str** → 改用 bars 最新交易日，回退墙钟

### 测试结果
- HMM tests: 39/39 passed
- Gate tests: 350/350 passed, 4 skipped
- 已知既有失败 `test_fetch_qfq_mootdx_fields`（数据源返回 20 而不是 3 根，非本次引起）

## 数据层 Bug 修复（第二轮）

### P0 修复
1. **has_today 墙钟日期** — `fetch_qfq_daily` 用 `datetime.now()` 判断是否含今日数据。改为从 bars 取最新交易日。
2. **Tushare 字段安全转换** — `float(r["amount"])` 在值为字符串 `"None"` 时 crash。`data_provider.py` 日线和周线都加了 `_safe_float()` 辅助函数规避。
3. **空 quote 降级检测** — `fetch_quote` 返回 `{}`（全源失败）时补录 `missing_sources`。

### P1 修复
4. **`print()` → `_logger.info()`** — `get_provider()` 改用统一日志等级。
5. **`warnings.warn()` → `_logger.warning()`** — `fetch_5m/15m/30m` fallback 提示走日志系统。
6. **冗余代码清理** — `except (ImportError, Exception)` → `except Exception`；`isinstance(x, list) and not isinstance(x, dict)` → `isinstance(x, list)`。

### 提交
- `3e6bdd9` — 第一轮 bug 修复（logger/HMM/signal_tracker/market_env）
- `5ee6f8d` — 数据层修复 6 项

## 后续同日修复（未全 commit）

1. **HMM 缩进回归** — `3e6bdd9` 误把 `detect_regime` 缩进进 `for`，首轮 `NameError` 被吞 → HMM 永不生效。已改回循环外（`market_env.py`）。
2. **回测引擎** — 买佣金双计去掉；时间止损仅 `vwap_mr`/`momentum_t0`；`entry_k`=成交棒；`scale_out` 半仓不占 rt。
3. **文档对齐** — `docs/t0-strategy-vwap-mr.md` 与引擎一致（止盈=回 VWAP；时间止损作用域；费用记账；`--scale-out`）；`t0-strategy-v2.md` 资产表同步；工作日志 `2026-07-26.md` 注明史实 vs 现行。

## 计算模块裁定项落地（feat/t0-v2-narrative）

双审计 + 裁定后全部改：
- VP：`report_builder` 改喂 `bars_5m`（不足则跳过）
- TRAIL：`structure_core` 近 `STRUCTURE_WINDOW` 最高收
- MF：真实资金流 `cum==0` 不再被 bars 估算覆盖
- CORR：`stage_state` 相关改收益率
- DROP5：`-5%` 先于 `-3%`
- FLOW0：零流入打断连续天数
- TODAY：`has_today` 用 `compute_data_freshness`
- HMM：`>=30` 才跑；今日价原地更新；`as_of` 缓存键；短序列低置信兜底
- MACD：按有效根数播种 EMA12/26
