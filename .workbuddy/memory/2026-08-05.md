# 2026-08-05 工作日志

## ST 双口径歧义修复（wyckoff_render.py，commit 待记录）

- 法源：`/Users/like/Documents/Workbuddy/Trader/wyckoff_ST双口径修复单_2026-08-05.md`
- 问题：引擎两个 ST 概念（st_signal=Spring支撑二次测试 / secondary_test_sc_signal=SC区回测）渲染层同灯名「二次测试」；Spring 确认亮但 ST（SC区回测）不亮 → 误以为缺环（南网 688248）。
- 改动：① `_EVENT_CN["ST"]`「二次测试」→「SC区回测」；② 三处「待 ST」→「待SC区回测」；③ ST 未亮+Spring 亮 → 说明行（daily+slim 双路径，slim 是 final_wyckoff 默认卡，修复单只列 daily 实测补 slim 才生效）。
- 测试：旧断言文案更新 14 行 + TestStScNoteS11 3 项；wyckoff 全量 407 passed；南网验收：`○ ST（SC区回测）`+Spring 灯+说明行+箱体「待SC区回测」全达标；12 安装位已 sync。
- ⚠️ 两个外部发现（未处理，报告用户）：
  1. **golden 基线在 HEAD 已过期**：600000 筹码行/「另N项」漂移，clean HEAD（全 stash）可复现，非本次引起（疑 mock 依赖本地缓存跨天刷新），未 capture。
  2. **工作区非本单既存改动**：data_provider.py/light_data.py/tushare_client.py（WorkBuddy tdx/mootdx 直连优先），疑似另一会话/用户在做的数据源偏好工作，未纳入本 commit。
