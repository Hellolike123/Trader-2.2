# 2026-07-25 工作日志

## ATR 自适应止损 + 参数扫描
- 止损从固定 0.5% 改为 ATR×1.5（自适应日线波动，上限 2%）
- 参数扫描：测试 40/50/60 分门槛 × 1.5/1.2/1.0 量比 → 40分/1.5x 最优
- 箱体距离从 ATR×0.5 放宽到 ×0.8 → 影响不大（20分权重低）
- ATR 止损后全池：三花+1.67% 天齐-0.05% 恩捷+0.44%

## Code Review 修复（P0×3 + P1×2 + chore）

**review 来源**：独立 agent 审查 fd830fc 提交，指出 6 项问题。

**P0-1 docstring 买入门槛不一致**：函数说明写"买入≥60"，实际实现≥40。已修正 docstring。

**P0-2 卖出条件未逐项触发**：代码要求累计 sell_score>=60 才卖，但文档说"评分>60 或 Close<VWAP 或接近箱顶"。改为 `sell_red = sell_score>=60 or close<vwap or near_box_top`。

**P0-3 渲染器未同步评分系统**：workbuddy 副本的 t0_core.py 仍是旧三重理论版。repo git 里是新版，拷贝覆盖 workbuddy。

**P1-1 ATR 用收盘价近似**：`_calc_atr_from_closes` 只用 |close-prev_close|，改为 True Range：`max(high-low, |high-prev_close|, |low-prev_close|)`。

**P1-2 入场说明不准确**：回测用下一棒开盘价成交，注释写"信号棒收盘价"。改注释为"下一棒开盘入场模拟收盘价成交"。

**chore**: .gitignore 添加 .mimocode（来自 review 提示）。commit 4f1f2fb。

## 科创50 回测 + 引擎致命 bug 修复（晚间）

**Bug**：`intraday_backtest_engine.py` 的 `run_one_day` 买入门控写成 `bstat == "已触发" and buy_green`，叠加旧威科夫/ICT 触发（`detect_buy_trigger`）与评分系统 `buy_green`。但用户最终策略是 5 条件评分（buy≥40），旧触发是三理论遗留。诊断 688009：buy_green 45.4% 但旧触发仅 6% → 取交集≈0 → 全市场 0 成交。

**修复**：买入门控改为仅 `buy_green`；卖出信号出场改为仅 `sell_red`，出场价用评分给的 `exit_price`(VWAP) 或信号棒收盘。引擎仅 `02-共享模块-shared/scripts/` 一份，已改（未 commit）。

**诊断数据**（688009）：EMA多头39% / Close>VWAP50% / 近箱底28% / 放量>1.5x18% / ATR>2%**0%**（科创板 5m ATR 普遍<2%，条件5几乎不亮）。buy.status：被阻断912 未进候选区275 观察中125 已触发88。

**Interim 结果**（688009 修正后）：147 笔 / 胜率19% / 收益-8.91% / 夏普-17.86。修正后能出信号，但胜率偏低、整体亏——策略结构问题（买在VWAP上、卖在VWAP下）。

**全量 50 只回测**：脚本 `/tmp/kcb50_aggregate.py`（直接 import run_backtest 聚合，输出 /tmp/kcb50_agg.csv + /tmp/kcb50_summary.json），后台跑（task Ff6RuS）。聚合口径：pooled_win=总胜场/总成交；pooled_ret=Σ期末权益/Σ投入-1；盈利股票占比。待完成后报整体胜率+盈率。

## bb_vwap 策略移植（用户给 backtrader T0InnerTrade 代码）

**用户给的 backtrader 代码**：EMA13/50+布林(20,2)+VWAP+威科夫SOW/LPS/Spring，正T(多头回踩VWAP+布林中下轨买，触上轨卖)+反T(空头高位卖回落买回)。

**评估**：策略逻辑比评分系统好（买回调卖强势，非买高卖低）。但 backtrader 直跑有6硬伤：settax不存在/无notify_order/止盈止损双卖单冲突/VWAP不按日重置/无底仓0交易/不模拟T+1涨跌停。且用户已拍板不用backtrader。

**移植方案**：在 `intraday_backtest_engine.py` 加 `--theory bb_vwap` 模式。`_bb_vwap_eval()` 实现正T信号脑（反T需底仓+做空，纯T0引擎不适用，裁掉）。signal_fn 在 bb_vwap 时跳过 build_price_point_model（更快更省内存），返回最小 model dict。SOW 条件修正：backtrader 原码 `close0>max(close1,close2) and close0<close1` 自相矛盾→改用 high0>max(high1,high2)。

**5m 标定（关键）**：原 backtrader 过滤门槛 BB宽2%/EMA粘合1.5% 面向日线，5m 上过滤99.8%棒。实测5m分布：BB宽 p50=1.2%/p90=2.6%，EMA gap p50=0.26%/p90=0.81%。重标定：BB_WIDTH_MIN=0.006、EMA_GAP_MIN=0.0008（仅过滤最压缩~10%）。

**3票预览（bb_vwap）**：688009 ret-5.30%/69笔/win17.4%；688036 ret-5.68%/67笔/win13.4%；688012 0笔。胜率仍偏低——结构改善但止盈(布林上轨)远/止损(信号棒低)近，回踩常延续下跌被打损。

**全量50只 bb_vwap 回测**：task Krj5xy，输出 /tmp/kcb50_bbvwap.csv。待完成报整体胜率+盈率。
